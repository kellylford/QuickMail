<Window x:Class="QuickMail.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuickMail.Views"
        xmlns:models="clr-namespace:QuickMail.Models"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:strings="clr-namespace:QuickMail.Resources"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}"
        Height="650" Width="1000"
        MinHeight="400" MinWidth="700"
        Background="{DynamicResource Theme.WindowBackground}"
        Foreground="{DynamicResource Theme.TextPrimary}"
        FontFamily="{DynamicResource Theme.FontFamily}"
        FontSize="{DynamicResource Theme.FontSizeBase}">

    <Window.Resources>
        <local:BoolToFontWeightConverter x:Key="BoolToWeightConverter"/>
        <BooleanToVisibilityConverter    x:Key="BoolToVisibilityConverter"/>
        <local:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        <local:BoolToColumnWidthConverter       x:Key="BoolToColumnWidthConverter"/>
        <local:StringToVisibilityConverter      x:Key="StringToVisibilityConverter"/>
        <local:StringToBrushConverter           x:Key="StringToBrushConverter"/>
        <local:MessageAccessibleNameConverter   x:Key="MessageAccessibleNameConverter"/>

        <!-- ── Shared context menus ── -->

        <!-- Sender-group context menu (root level of SenderGroupTree) -->
        <ContextMenu x:Key="SenderGroupContextMenu"
                     AutomationProperties.Name="{x:Static strings:Strings.MainWindow_SenderGroupActions}"
                     DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
            <MenuItem Header="{x:Static strings:Strings.MainWindow_Reply}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_ReplyName}"
                      Command="{Binding ReplySenderGroupCommand}"
                      CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_ReplyAll}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_ReplyAllName}"
                      Command="{Binding ReplyAllSenderGroupCommand}"
                      CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_Forward}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_ForwardName}"
                      Command="{Binding ForwardSenderGroupCommand}"
                      CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
            <Separator/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_MoveToFolderEllipsis}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_MoveSenderGroupToFolder}"
                      Click="SenderGroupContextMenu_MoveToFolder_Click"/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_CopyToFolderEllipsis}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_CopySenderGroupToFolder}"
                      Click="SenderGroupContextMenu_CopyToFolder_Click"/>
            <Separator/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_DeleteAllFromSender}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_DeleteAllFromSenderName}"
                      Command="{Binding DeleteSenderGroupCommand}"
                      CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
            <Separator/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_Flags}"
                      Tag="FlagsSubmenu"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_FlagsName}"/>
            <Separator/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_ExpandGroup}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_ExpandGroupName}"
                      Command="{Binding ExpandSenderGroupCommand}"
                      CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_CollapseGroup}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_CollapseGroupName}"
                      Command="{Binding CollapseSenderGroupCommand}"
                      CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
            <Separator/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_ExpandAllGroups}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_ExpandAllGroupsName}"
                      Command="{Binding ExpandAllSenderGroupsCommand}"/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_CollapseAllGroups}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_CollapseAllGroupsName}"
                      Command="{Binding CollapseAllSenderGroupsCommand}"/>
        </ContextMenu>

        <ContextMenu x:Key="ToGroupContextMenu"
                     AutomationProperties.Name="{x:Static strings:Strings.MainWindow_RecipientGroupActions}"
                     DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
            <MenuItem Header="{x:Static strings:Strings.MainWindow_Reply}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_ReplyName}"
                      Command="{Binding ReplyToGroupCommand}"
                      CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_ReplyAll}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_ReplyAllName}"
                      Command="{Binding ReplyAllToGroupCommand}"
                      CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_Forward}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_ForwardName}"
                      Command="{Binding ForwardToGroupCommand}"
                      CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
            <Separator/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_MoveToFolderEllipsis}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_MoveRecipientGroupToFolder}"
                      Click="ToGroupContextMenu_MoveToFolder_Click"/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_CopyToFolderEllipsis}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_CopyRecipientGroupToFolder}"
                      Click="ToGroupContextMenu_CopyToFolder_Click"/>
            <Separator/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_DeleteAllToRecipient}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_DeleteAllToRecipientName}"
                      Command="{Binding DeleteToGroupCommand}"
                      CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
            <Separator/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_Flags}"
                      Tag="FlagsSubmenu"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_FlagsName}"/>
            <Separator/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_ExpandGroup}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_ExpandGroupName}"
                      Command="{Binding ExpandToGroupCommand}"
                      CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_CollapseGroup}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_CollapseGroupName}"
                      Command="{Binding CollapseToGroupCommand}"
                      CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
            <Separator/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_ExpandAllGroups}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_ExpandAllGroupsName}"
                      Command="{Binding ExpandAllToGroupsCommand}"/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_CollapseAllGroups}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_CollapseAllGroupsName}"
                      Command="{Binding CollapseAllToGroupsCommand}"/>
        </ContextMenu>

        <!-- Conversation-group context menu (root level of ConversationTree) -->
        <ContextMenu x:Key="ConversationGroupContextMenu"
                     AutomationProperties.Name="{x:Static strings:Strings.MainWindow_ConversationActions}"
                     DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
            <MenuItem Header="{x:Static strings:Strings.MainWindow_Reply}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_ReplyName}"
                      Command="{Binding ReplyConversationCommand}"
                      CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_ReplyAll}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_ReplyAllName}"
                      Command="{Binding ReplyAllConversationCommand}"
                      CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_Forward}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_ForwardName}"
                      Command="{Binding ForwardConversationCommand}"
                      CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
            <Separator/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_MoveToFolderEllipsis}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_MoveConversationToFolder}"
                      Click="ConversationContextMenu_MoveToFolder_Click"/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_CopyToFolderEllipsis}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_CopyConversationToFolder}"
                      Click="ConversationContextMenu_CopyToFolder_Click"/>
            <Separator/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_DeleteConversation}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_DeleteConversationName}"
                      Command="{Binding DeleteConversationCommand}"
                      CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
            <Separator/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_Flags}"
                      Tag="FlagsSubmenu"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_FlagsName}"/>
            <Separator/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_ExpandConversation}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_ExpandConversationName}"
                      Command="{Binding ExpandConversationCommand}"
                      CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_CollapseConversation}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_CollapseConversationName}"
                      Command="{Binding CollapseConversationCommand}"
                      CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
            <Separator/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_ExpandAllConversations}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_ExpandAllConversationsName}"
                      Command="{Binding ExpandAllConversationsCommand}"/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_CollapseAllConversations}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_CollapseAllConversationsName}"
                      Command="{Binding CollapseAllConversationsCommand}"/>
        </ContextMenu>

        <!-- Individual-message context menu (MessageList and child nodes in ConversationTree) -->
        <ContextMenu x:Key="MessageContextMenu"
                     AutomationProperties.Name="{x:Static strings:Strings.MainWindow_MessageActions}"
                     DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
            <MenuItem Header="{x:Static strings:Strings.MainWindow_Reply}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_ReplyName}"
                      Command="{Binding ReplyCommand}"/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_ReplyAll}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_ReplyAllName}"
                      Command="{Binding ReplyAllCommand}"/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_Forward}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_ForwardName}"
                      Command="{Binding ForwardCommand}"/>
            <Separator/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_MoveToFolderEllipsis}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_MoveToFolderName}"
                      Click="MessageContextMenu_MoveToFolder_Click"/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_CopyToFolderEllipsis}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_CopyToFolderName}"
                      Click="MessageContextMenu_CopyToFolder_Click"/>
            <Separator/>
            <MenuItem Header="{x:Static strings:Strings.Common_Delete}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_DeleteMessageName}"
                      Command="{Binding DeleteMessageCommand}"/>
            <Separator/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_Flags}"
                      Tag="FlagsSubmenu"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_FlagsName}"/>
            <Separator/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_CreateRuleFromMessageEllipsis}"
                      InputGestureText="Ctrl+Shift+T"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_CreateRuleFromMessageName}"
                      AutomationProperties.AcceleratorKey="Ctrl+Shift+T"
                      Command="{Binding CreateRuleFromMessageCommand}"/>
        </ContextMenu>

        <!-- Folder context menu — MUST live in Window.Resources, not inside a Style.Setter.Value.
             The WPF XAML compiler generates incorrect type casts for Click handlers inside
             Style setters (it emits GridSplitter instead of MenuItem), causing an APPCRASH. -->
        <ContextMenu x:Key="FolderContextMenu"
                     AutomationProperties.Name="{x:Static strings:Strings.MainWindow_FolderActions}">
            <MenuItem Header="{x:Static strings:Strings.MainWindow_NewFolderEllipsis}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_NewFolderName}"
                      Click="FolderContextMenu_NewFolder_Click"/>
            <Separator/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_MoveFolderEllipsis}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_MoveFolderName}"
                      Click="FolderContextMenu_MoveFolder_Click"/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_CopyFolderEllipsis}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_CopyFolderName}"
                      Click="FolderContextMenu_CopyFolder_Click"/>
            <Separator/>
            <MenuItem Header="{x:Static strings:Strings.MainWindow_DeleteFolder}"
                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_DeleteFolderName}"
                      Click="FolderContextMenu_DeleteFolder_Click"/>
        </ContextMenu>
    </Window.Resources>

    <Window.InputBindings>
        <!-- Navigation shortcuts not in the command registry are handled directly in PreviewKeyDown. -->
        <!-- Action command shortcuts are routed through the CommandRegistry in OnWindowKeyDown.     -->
    </Window.InputBindings>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- Menu bar -->
            <RowDefinition Height="Auto"/>  <!-- Toolbar -->
            <RowDefinition Height="*"/>     <!-- Content -->
            <RowDefinition Height="Auto"/>  <!-- Status bar -->
        </Grid.RowDefinitions>

        <!-- ── Menu bar — Row 0 ── -->
        <Menu x:Name="MainMenuBar"
              Grid.Row="0"
              IsTabStop="False"
              KeyboardNavigation.TabNavigation="None"
              AutomationProperties.Name="{x:Static strings:Strings.MainWindow_MainMenu}">

            <!-- File -->
            <MenuItem Header="{x:Static strings:Strings.MainWindow_File}">
                <MenuItem Header="{x:Static strings:Strings.MainWindow_NewMessage}"
                          Command="{Binding NewMessageCommand}"
                          InputGestureText="Ctrl+N"/>
                <Separator/>
                <MenuItem Header="{x:Static strings:Strings.MainWindow_ManageAccountsEllipsis}"
                          Command="{Binding ManageAccountsCommand}"/>
                <Separator/>
                <MenuItem Header="{x:Static strings:Strings.MainWindow_SettingsEllipsis}"
                          Click="MenuSettings_Click"
                          InputGestureText="Ctrl+,"/>
                <Separator/>
                <MenuItem Header="{x:Static strings:Strings.MainWindow_Exit}"
                          Command="{Binding ExitCommand}"/>
            </MenuItem>

            <!-- Message -->
            <MenuItem Header="{x:Static strings:Strings.MainWindow_MessageMenu}">
                <MenuItem Header="{x:Static strings:Strings.MainWindow_Reply}"
                          Command="{Binding ReplyCommand}"
                          InputGestureText="Ctrl+R"/>
                <MenuItem Header="{x:Static strings:Strings.MainWindow_ReplyAll}"
                          Command="{Binding ReplyAllCommand}"
                          InputGestureText="Ctrl+Shift+R"/>
                <MenuItem Header="{x:Static strings:Strings.MainWindow_Forward}"
                          Command="{Binding ForwardCommand}"
                          InputGestureText="Ctrl+F"/>
                <Separator/>
                <MenuItem Header="{x:Static strings:Strings.Common_Delete}"
                          Command="{Binding DeleteMessageCommand}"
                          InputGestureText="Del"/>
                <MenuItem Header="{x:Static strings:Strings.MainWindow_EmptyTrash}"
                          Command="{Binding EmptyTrashCommand}"
                          InputGestureText="Ctrl+Shift+E"/>
                <Separator/>
                <!-- Keep IsEnabled off these three items so they remain keyboard-navigable
                     per Windows convention (dimmed items are still focusable). Visual dimming
                     is applied via DataTrigger; the Click handlers guard against invalid state. -->
                <MenuItem Header="{x:Static strings:Strings.MainWindow_MoveToFolderEllipsis}"
                          Click="MenuMoveToFolder_Click">
                    <MenuItem.Style>
                        <Style TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasSelectedMessage}" Value="False">
                                    <Setter Property="Foreground" Value="{DynamicResource Theme.TextDisabled}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </MenuItem.Style>
                </MenuItem>
                <MenuItem Header="{x:Static strings:Strings.MainWindow_CopyToFolderEllipsis}"
                          Click="MenuCopyToFolder_Click">
                    <MenuItem.Style>
                        <Style TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasSelectedMessage}" Value="False">
                                    <Setter Property="Foreground" Value="{DynamicResource Theme.TextDisabled}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </MenuItem.Style>
                </MenuItem>
                <Separator/>
                <MenuItem Header="{x:Static strings:Strings.MainWindow_GrabAddressesFromMessage}"
                          Click="MenuGrabAddresses_Click"
                          InputGestureText="Ctrl+Shift+G">
                    <MenuItem.Style>
                        <Style TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsMessageOpen}" Value="False">
                                    <Setter Property="Foreground" Value="{DynamicResource Theme.TextDisabled}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </MenuItem.Style>
                </MenuItem>
            </MenuItem>

            <!-- View -->
            <MenuItem Header="{x:Static strings:Strings.MainWindow_View}">
                <MenuItem Header="{x:Static strings:Strings.MainWindow_Refresh}"
                          Command="{Binding RefreshCommand}"
                          InputGestureText="F5"/>
                <Separator/>
                <MenuItem Header="{x:Static strings:Strings.MainWindow_ViewMode}">
                    <MenuItem Header="{x:Static strings:Strings.MainWindow_MessagesMode}"
                              IsCheckable="True"
                              IsChecked="{Binding IsMessagesView, Mode=OneWay}"
                              Command="{Binding SetViewModeCommand}"
                              CommandParameter="Messages"/>
                    <MenuItem Header="{x:Static strings:Strings.MainWindow_ConversationsMode}"
                              IsCheckable="True"
                              IsChecked="{Binding IsConversationsView, Mode=OneWay}"
                              Command="{Binding SetViewModeCommand}"
                              CommandParameter="Conversations"/>
                    <MenuItem Header="{x:Static strings:Strings.MainWindow_BySender}"
                              IsCheckable="True"
                              IsChecked="{Binding IsFromView, Mode=OneWay}"
                              Command="{Binding SetViewModeCommand}"
                              CommandParameter="From"/>
                    <MenuItem Header="{x:Static strings:Strings.MainWindow_ByRecipient}"
                              IsCheckable="True"
                              IsChecked="{Binding IsToView, Mode=OneWay}"
                              Command="{Binding SetViewModeCommand}"
                              CommandParameter="To"/>
                </MenuItem>
                <MenuItem Header="{x:Static strings:Strings.MainWindow_Filter}">
                    <MenuItem Header="{x:Static strings:Strings.MainWindow_ShowAll}"
                              IsCheckable="True"
                              IsChecked="{Binding IsFilterAll, Mode=OneWay}"
                              Command="{Binding SetFilterCommand}"
                              CommandParameter="all"/>
                    <Separator/>
                    <MenuItem Header="{x:Static strings:Strings.MainWindow_Unread}"
                              IsCheckable="True"
                              IsChecked="{Binding IsFilterUnread, Mode=OneWay}"
                              Command="{Binding SetFilterCommand}"
                              CommandParameter="unread"/>
                    <MenuItem Header="{x:Static strings:Strings.MainWindow_Read}"
                              IsCheckable="True"
                              IsChecked="{Binding IsFilterRead, Mode=OneWay}"
                              Command="{Binding SetFilterCommand}"
                              CommandParameter="read"/>
                    <Separator/>
                    <MenuItem Header="{x:Static strings:Strings.MainWindow_WithAttachments}"
                              IsCheckable="True"
                              IsChecked="{Binding IsFilterWithAttachments, Mode=OneWay}"
                              Command="{Binding SetFilterCommand}"
                              CommandParameter="attachments"/>
                    <MenuItem Header="{x:Static strings:Strings.MainWindow_Replied}"
                              IsCheckable="True"
                              IsChecked="{Binding IsFilterReplied, Mode=OneWay}"
                              Command="{Binding SetFilterCommand}"
                              CommandParameter="replied"/>
                    <MenuItem Header="{x:Static strings:Strings.MainWindow_Forwarded}"
                              IsCheckable="True"
                              IsChecked="{Binding IsFilterForwarded, Mode=OneWay}"
                              Command="{Binding SetFilterCommand}"
                              CommandParameter="forwarded"/>
                    <Separator/>
                    <MenuItem Header="{x:Static strings:Strings.MainWindow_ToMe}"
                              IsCheckable="True"
                              IsChecked="{Binding IsFilterToMe, Mode=OneWay}"
                              Command="{Binding SetFilterCommand}"
                              CommandParameter="tome"/>
                    <Separator/>
                    <MenuItem x:Name="FilterFlaggedItem"
                              Header="{x:Static strings:Strings.MainWindow_Flagged}"
                              IsChecked="{Binding IsFilterFlagged, Mode=OneWay}">
                        <MenuItem Header="{x:Static strings:Strings.MainWindow_AllFlagged}"
                                  IsCheckable="True"
                                  IsChecked="{Binding IsFilterAllFlagged, Mode=OneWay}"
                                  Command="{Binding SetFilterCommand}"
                                  CommandParameter="flagged"/>
                        <Separator x:Name="FilterFlaggedSeparator"/>
                    </MenuItem>
                </MenuItem>
                <MenuItem Header="{x:Static strings:Strings.MainWindow_Sort}">
                    <MenuItem Header="{x:Static strings:Strings.MainWindow_NewestFirst}"
                              IsCheckable="True"
                              IsChecked="{Binding IsSortDateDesc, Mode=OneWay}"
                              Command="{Binding SetSortCommand}"
                              CommandParameter="dateDesc"/>
                    <MenuItem Header="{x:Static strings:Strings.MainWindow_OldestFirst}"
                              IsCheckable="True"
                              IsChecked="{Binding IsSortDateAsc, Mode=OneWay}"
                              Command="{Binding SetSortCommand}"
                              CommandParameter="dateAsc"/>
                    <Separator/>
                    <MenuItem Header="{x:Static strings:Strings.MainWindow_AtoZ}"
                              IsCheckable="True"
                              IsChecked="{Binding IsSortAlphaAsc, Mode=OneWay}"
                              Command="{Binding SetSortCommand}"
                              CommandParameter="alphaAsc"/>
                    <MenuItem Header="{x:Static strings:Strings.MainWindow_ZtoA}"
                              IsCheckable="True"
                              IsChecked="{Binding IsSortAlphaDesc, Mode=OneWay}"
                              Command="{Binding SetSortCommand}"
                              CommandParameter="alphaDesc"/>
                    <Separator/>
                    <MenuItem Header="{x:Static strings:Strings.MainWindow_FlaggedFirst}"
                              IsCheckable="True"
                              IsChecked="{Binding IsSortFlaggedFirst, Mode=OneWay}"
                              Command="{Binding SetSortCommand}"
                              CommandParameter="flaggedFirst"/>
                    <Separator/>
                    <MenuItem Header="{x:Static strings:Strings.MainWindow_MostMessages}"
                              IsCheckable="True"
                              IsChecked="{Binding IsSortCountDesc, Mode=OneWay}"
                              IsEnabled="{Binding IsCountSortAvailable}"
                              Command="{Binding SetSortCommand}"
                              CommandParameter="countDesc"/>
                    <MenuItem Header="{x:Static strings:Strings.MainWindow_FewestMessages}"
                              IsCheckable="True"
                              IsChecked="{Binding IsSortCountAsc, Mode=OneWay}"
                              IsEnabled="{Binding IsCountSortAvailable}"
                              Command="{Binding SetSortCommand}"
                              CommandParameter="countAsc"/>
                </MenuItem>
                <MenuItem Header="{x:Static strings:Strings.MainWindow_Views}" x:Name="ViewsMenuItem">
                    <!-- Dynamic view items inserted by code-behind -->
                    <Separator x:Name="ViewsMenuSeparator" Visibility="Collapsed"/>
                    <MenuItem Header="{x:Static strings:Strings.MainWindow_SaveViewEllipsis}"
                              Command="{Binding SaveViewCommand}"
                              AutomationProperties.Name="{x:Static strings:Strings.MainWindow_SaveViewName}"/>
                    <MenuItem Header="{x:Static strings:Strings.MainWindow_ManageViewsEllipsis}"
                              Command="{Binding ManageViewsCommand}"
                              AutomationProperties.Name="{x:Static strings:Strings.MainWindow_ManageViewsName}"/>
                    <MenuItem Header="{x:Static strings:Strings.MainWindow_ClearView}"
                              Command="{Binding ClearViewCommand}"
                              AutomationProperties.Name="{x:Static strings:Strings.MainWindow_ClearViewName}"/>
                </MenuItem>
                <MenuItem Header="{x:Static strings:Strings.MainWindow_SyncRange}">
                    <MenuItem Header="{x:Static strings:Strings.MainWindow_Last7Days}"
                              IsCheckable="True"
                              IsChecked="{Binding IsSyncDays7, Mode=OneWay}"
                              Command="{Binding SetSyncDaysCommand}"
                              CommandParameter="7"/>
                    <MenuItem Header="{x:Static strings:Strings.MainWindow_Last30Days}"
                              IsCheckable="True"
                              IsChecked="{Binding IsSyncDays30, Mode=OneWay}"
                              Command="{Binding SetSyncDaysCommand}"
                              CommandParameter="30"/>
                    <MenuItem Header="{x:Static strings:Strings.MainWindow_Last6Months}"
                              IsCheckable="True"
                              IsChecked="{Binding IsSyncDays180, Mode=OneWay}"
                              Command="{Binding SetSyncDaysCommand}"
                              CommandParameter="180"/>
                    <MenuItem Header="{x:Static strings:Strings.MainWindow_Last1Year}"
                              IsCheckable="True"
                              IsChecked="{Binding IsSyncDays365, Mode=OneWay}"
                              Command="{Binding SetSyncDaysCommand}"
                              CommandParameter="365"/>
                    <Separator/>
                    <MenuItem Header="{x:Static strings:Strings.MainWindow_AllMail}"
                              IsCheckable="True"
                              IsChecked="{Binding IsSyncDaysAll, Mode=OneWay}"
                              Command="{Binding SetSyncDaysCommand}"
                              CommandParameter="0"/>
                </MenuItem>
                <Separator/>
                <MenuItem Header="{x:Static strings:Strings.MainWindow_GoToFolderEllipsis}"
                          Click="MenuFolderPicker_Click"/>
                <MenuItem Header="{x:Static strings:Strings.MainWindow_SearchFoldersEllipsis}"
                          Click="MenuSearchFolders_Click"
                          InputGestureText="Ctrl+Shift+F"/>
            </MenuItem>

            <!-- Tools — always available (mirrors the Compose window's Tools menu).
                 Each command lives here only; not duplicated in File/Message/View. -->
            <MenuItem Header="{x:Static strings:Strings.MainWindow_Tools}">
                <MenuItem Header="{x:Static strings:Strings.MainWindow_ManageThemesEllipsis}"
                          Click="MenuManageThemes_Click"
                          AutomationProperties.Name="{x:Static strings:Strings.MainWindow_ManageThemesName}"/>
                <MenuItem Header="{x:Static strings:Strings.MainWindow_NextTheme}"
                          Click="MenuNextTheme_Click"/>
                <MenuItem Header="{x:Static strings:Strings.MainWindow_PreviousTheme}"
                          Click="MenuPreviousTheme_Click"/>
                <Separator/>
                <MenuItem Header="{x:Static strings:Strings.MainWindow_AddressBookEllipsis}"
                          Click="MenuAddressBook_Click"
                          InputGestureText="Ctrl+Shift+B"/>
                <MenuItem Header="{x:Static strings:Strings.MainWindow_RulesEllipsis}"
                          Command="{Binding OpenRulesManagerCommand}"
                          InputGestureText="Ctrl+Shift+L"
                          AutomationProperties.Name="{x:Static strings:Strings.MainWindow_ManageRulesName}"
                          AutomationProperties.AcceleratorKey="Ctrl+Shift+L"/>
                <Separator/>
                <MenuItem Header="{x:Static strings:Strings.MainWindow_CommandPaletteEllipsis}"
                          Click="MenuCommandPalette_Click"
                          InputGestureText="Ctrl+Shift+P"/>
            </MenuItem>

            <!-- Help -->
            <MenuItem Header="{x:Static strings:Strings.MainWindow_Help}">
                <MenuItem Header="{Binding UpdateAvailableText, Mode=OneWay}"
                          Command="{Binding OpenUpdatePageCommand}"/>
                <Separator/>
                <MenuItem Header="{x:Static strings:Strings.MainWindow_UserGuide}"
                          Command="{Binding ViewUserGuideCommand}"
                          InputGestureText="F1"/>
                <MenuItem Header="{x:Static strings:Strings.MainWindow_KeyboardTutorial}"
                          AutomationProperties.Name="{x:Static strings:Strings.MainWindow_KeyboardTutorialName}"
                          Click="MenuKeyboardTutorial_Click"/>
                <Separator/>
                <MenuItem Header="{x:Static strings:Strings.MainWindow_ReportABug}"
                          AutomationProperties.Name="{x:Static strings:Strings.MainWindow_ReportABugName}"
                          Click="MenuReportBug_Click"/>
                <Separator/>
                <MenuItem Header="{x:Static strings:Strings.MainWindow_AboutQuickMail}"
                          AutomationProperties.Name="{x:Static strings:Strings.MainWindow_AboutQuickMailName}"
                          Click="MenuAbout_Click"/>
            </MenuItem>

        </Menu>

        <!-- ── Toolbar — Ctrl+0 only; excluded from Tab sequence ── -->
        <ToolBar x:Name="MainToolbar"
                 Grid.Row="1"
                 AutomationProperties.Name="{x:Static strings:Strings.MainWindow_MainToolbar}"
                 KeyboardNavigation.TabNavigation="None"
                 KeyboardNavigation.DirectionalNavigation="Cycle"
                 PreviewKeyDown="Toolbar_PreviewKeyDown">
            <Button x:Name="ToolbarFirstButton"
                    Style="{DynamicResource ToolbarButton}"
                    Command="{Binding NewMessageCommand}"
                    AutomationProperties.Name="{x:Static strings:Strings.MainWindow_NewMessageName}"
                    AutomationProperties.AcceleratorKey="Ctrl+N"
                    ToolTip="{x:Static strings:Strings.MainWindow_NewMessageToolTip}"
                    Content="{x:Static strings:Strings.MainWindow_NewButton}"/>
            <Separator/>
            <Button Style="{DynamicResource ToolbarButton}"
                    Command="{Binding ReplyCommand}"
                    AutomationProperties.Name="{x:Static strings:Strings.MainWindow_ReplyName}"
                    AutomationProperties.AcceleratorKey="Ctrl+R"
                    ToolTip="{x:Static strings:Strings.MainWindow_ReplyToolTip}"
                    Content="{x:Static strings:Strings.MainWindow_Reply}"/>
            <Button Style="{DynamicResource ToolbarButton}"
                    Command="{Binding ReplyAllCommand}"
                    AutomationProperties.Name="{x:Static strings:Strings.MainWindow_ReplyAllName}"
                    AutomationProperties.AcceleratorKey="Ctrl+Shift+R"
                    ToolTip="{x:Static strings:Strings.MainWindow_ReplyAllToolTip}"
                    Content="{x:Static strings:Strings.MainWindow_ReplyAll}"/>
            <Button Style="{DynamicResource ToolbarButton}"
                    Command="{Binding ForwardCommand}"
                    AutomationProperties.Name="{x:Static strings:Strings.MainWindow_ForwardName}"
                    AutomationProperties.AcceleratorKey="Ctrl+F"
                    ToolTip="{x:Static strings:Strings.MainWindow_ForwardToolTip}"
                    Content="{x:Static strings:Strings.MainWindow_Forward}"/>
            <Separator/>
            <Button Style="{DynamicResource ToolbarButton}"
                    Command="{Binding DeleteMessageCommand}"
                    AutomationProperties.Name="{x:Static strings:Strings.MainWindow_DeleteMessageName}"
                    AutomationProperties.AcceleratorKey="Delete"
                    ToolTip="{x:Static strings:Strings.MainWindow_DeleteToolTip}"
                    Content="{x:Static strings:Strings.Common_Delete}"/>
            <Separator/>
            <Button Style="{DynamicResource ToolbarButton}"
                    Command="{Binding RefreshCommand}"
                    AutomationProperties.Name="{x:Static strings:Strings.MainWindow_Refresh}"
                    AutomationProperties.AcceleratorKey="F5"
                    ToolTip="{x:Static strings:Strings.MainWindow_RefreshToolTip}"
                    Content="{x:Static strings:Strings.MainWindow_Refresh}"/>
            <Button x:Name="SyncRangeButton"
                    Style="{DynamicResource ToolbarButton}"
                    Content="{Binding SyncRangeLabel}"
                    AutomationProperties.Name="{x:Static strings:Strings.MainWindow_SyncRangeName}"
                    ToolTip="{x:Static strings:Strings.MainWindow_SyncRangeToolTip}"
                    Click="SyncRangeButton_Click">
                <Button.ContextMenu>
                    <ContextMenu AutomationProperties.Name="{x:Static strings:Strings.MainWindow_SyncRangeName}"
                                 DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                        <MenuItem Header="{x:Static strings:Strings.MainWindow_Last7Days}"
                                  IsCheckable="True"
                                  IsChecked="{Binding IsSyncDays7, Mode=OneWay}"
                                  Command="{Binding SetSyncDaysCommand}"
                                  CommandParameter="7"/>
                        <MenuItem Header="{x:Static strings:Strings.MainWindow_Last30Days}"
                                  IsCheckable="True"
                                  IsChecked="{Binding IsSyncDays30, Mode=OneWay}"
                                  Command="{Binding SetSyncDaysCommand}"
                                  CommandParameter="30"/>
                        <MenuItem Header="{x:Static strings:Strings.MainWindow_Last6MonthsLong}"
                                  IsCheckable="True"
                                  IsChecked="{Binding IsSyncDays180, Mode=OneWay}"
                                  Command="{Binding SetSyncDaysCommand}"
                                  CommandParameter="180"/>
                        <MenuItem Header="{x:Static strings:Strings.MainWindow_Last1YearLong}"
                                  IsCheckable="True"
                                  IsChecked="{Binding IsSyncDays365, Mode=OneWay}"
                                  Command="{Binding SetSyncDaysCommand}"
                                  CommandParameter="365"/>
                        <Separator/>
                        <MenuItem Header="{x:Static strings:Strings.MainWindow_AllMail}"
                                  IsCheckable="True"
                                  IsChecked="{Binding IsSyncDaysAll, Mode=OneWay}"
                                  Command="{Binding SetSyncDaysCommand}"
                                  CommandParameter="0"/>
                    </ContextMenu>
                </Button.ContextMenu>
            </Button>
            <Separator/>
            <Button x:Name="ViewModeButton"
                    Style="{DynamicResource ToolbarButton}"
                    Content="{Binding ViewModeLabel}"
                    AutomationProperties.Name="{x:Static strings:Strings.MainWindow_ViewModeName}"
                    ToolTip="{x:Static strings:Strings.MainWindow_ViewModeToolTip}"
                    Click="ViewModeButton_Click">
                <Button.ContextMenu>
                    <ContextMenu AutomationProperties.Name="{x:Static strings:Strings.MainWindow_ViewModeName}"
                                 DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                        <MenuItem Header="{x:Static strings:Strings.MainWindow_MessagesMode}"
                                  IsCheckable="True"
                                  IsChecked="{Binding IsMessagesView, Mode=OneWay}"
                                  Command="{Binding SetViewModeCommand}"
                                  CommandParameter="Messages"/>
                        <MenuItem Header="{x:Static strings:Strings.MainWindow_ConversationsMode}"
                                  IsCheckable="True"
                                  IsChecked="{Binding IsConversationsView, Mode=OneWay}"
                                  Command="{Binding SetViewModeCommand}"
                                  CommandParameter="Conversations"/>
                        <MenuItem Header="{x:Static strings:Strings.MainWindow_From}"
                                  IsCheckable="True"
                                  IsChecked="{Binding IsFromView, Mode=OneWay}"
                                  Command="{Binding SetViewModeCommand}"
                                  CommandParameter="From"/>
                        <MenuItem Header="{x:Static strings:Strings.MainWindow_To}"
                                  IsCheckable="True"
                                  IsChecked="{Binding IsToView, Mode=OneWay}"
                                  Command="{Binding SetViewModeCommand}"
                                  CommandParameter="To"/>
                    </ContextMenu>
                </Button.ContextMenu>
            </Button>
            <Separator/>
            <Button Style="{DynamicResource ToolbarButton}"
                    Command="{Binding EmptyTrashCommand}"
                    AutomationProperties.Name="{x:Static strings:Strings.MainWindow_EmptyTrash}"
                    AutomationProperties.AcceleratorKey="Ctrl+Shift+E"
                    ToolTip="{x:Static strings:Strings.MainWindow_EmptyTrashToolTip}"
                    Content="{x:Static strings:Strings.MainWindow_EmptyTrash}"/>
            <Separator/>
            <Button Style="{DynamicResource ToolbarButton}"
                    Command="{Binding ManageAccountsCommand}"
                    AutomationProperties.Name="{x:Static strings:Strings.MainWindow_ManageEmailAccountsName}"
                    ToolTip="{x:Static strings:Strings.MainWindow_ManageEmailAccountsName}"
                    Content="{x:Static strings:Strings.MainWindow_Accounts}"/>
            <Separator/>
            <Button Style="{DynamicResource ToolbarButton}"
                    Command="{Binding ViewUserGuideCommand}"
                    AutomationProperties.Name="{x:Static strings:Strings.MainWindow_ViewUserGuideName}"
                    AutomationProperties.AcceleratorKey="F1"
                    ToolTip="{x:Static strings:Strings.MainWindow_ViewUserGuideToolTip}"
                    Content="{x:Static strings:Strings.MainWindow_UserGuideButton}"/>
        </ToolBar>

        <!-- ── Three-pane layout (Outlook-style): Accounts+Folders left | Messages right ── -->
        <!--  F6 pane ring: AccountList(1) → FolderList(2) → MessageList(3) → Toolbar → StatusBar → wrap -->
        <!--  Arrow keys navigate within the focused pane (TabNavigation=Once on each)      -->
        <!--  Enter triggers action; focus never changes unless user does it                 -->
        <!--  Ctrl+0 = toolbar | Ctrl+1/2/3 = jump directly to any pane                    -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" MinWidth="120"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*" MinWidth="300"/>
            </Grid.ColumnDefinitions>

            <!-- Left pane: Accounts on top, Folders below -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"  MinHeight="60"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="2*" MinHeight="80"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="{x:Static strings:Strings.MainWindow_Accounts}" FontWeight="SemiBold" Padding="6,4"
                           Focusable="False"
                           Background="{DynamicResource Theme.ChromeBackground}"/>

                <ListBox Grid.Row="1"
                         x:Name="AccountList"
                         AutomationProperties.Name="{x:Static strings:Strings.MainWindow_EmailAccountsName}"
                         ItemsSource="{Binding Accounts}"
                         SelectedItem="{Binding SelectedAccount, Mode=TwoWay}"
                         TextSearch.TextPath="AccountLabel"
                         BorderThickness="0,0,1,0"
                         TabIndex="1"
                         KeyboardNavigation.TabNavigation="Once"
                         GotKeyboardFocus="List_GotKeyboardFocus"
                         PreviewKeyDown="AccountList_PreviewKeyDown">

                    <!-- Two-line item: account name + live status (connected/disconnected + inbox counts).
                         AutomationProperties.Name on the container announces the full detail to screen
                         readers on focus, without requiring the user to hover over the tooltip. -->
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="AutomationProperties.Name" Value="{Binding AccessibleName}"/>
                            <Setter Property="Padding"                   Value="4,4,4,4"/>
                        </Style>
                    </ListBox.ItemContainerStyle>

                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <!-- AriaHiddenTextBlock suppresses both child elements from the UIA tree.
                                 The full accessible name is on the ListBoxItem container via
                                 AutomationProperties.Name → AccessibleName, so screen readers
                                 read it once only. -->
                            <StackPanel>
                                <local:AriaHiddenTextBlock Text="{Binding AccountLabel}"/>
                                <local:AriaHiddenTextBlock Text="{Binding StatusLabel}"
                                                           Foreground="{DynamicResource Theme.TextSecondary}"
                                                           FontSize="{DynamicResource Theme.FontSizeSmall}"
                                                           Margin="0,1,0,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>

                    <ListBox.ContextMenu>
                        <ContextMenu AutomationProperties.Name="{x:Static strings:Strings.MainWindow_AccountActions}">
                            <MenuItem Header="{x:Static strings:Strings.MainWindow_DeleteAccount}"
                                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_DeleteAccountName}"
                                      Command="{Binding PlacementTarget.DataContext.DeleteAccountCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                      CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                            <MenuItem Header="{x:Static strings:Strings.MainWindow_AccountSettingsEllipsis}"
                                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_AccountSettingsName}"
                                      Command="{Binding PlacementTarget.DataContext.OpenAccountSettingsCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                      CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                        </ContextMenu>
                    </ListBox.ContextMenu>
                </ListBox>

                <TextBlock Grid.Row="2" Text="{x:Static strings:Strings.MainWindow_Folders}" FontWeight="SemiBold" Padding="6,4"
                           Focusable="False"
                           Background="{DynamicResource Theme.ChromeBackground}"/>

                <!-- True TreeView so screen readers announce level, expanded/collapsed state correctly. -->
                <TreeView Grid.Row="3"
                          x:Name="FolderList"
                          AutomationProperties.Name="{x:Static strings:Strings.MainWindow_FolderListName}"
                          ItemsSource="{Binding FolderTree}"
                          BorderThickness="0,0,1,0"
                          TabIndex="2"
                          KeyboardNavigation.TabNavigation="Once"
                          GotKeyboardFocus="FolderList_GotKeyboardFocus"
                          PreviewTextInput="FolderList_PreviewTextInput"
                          PreviewKeyDown="FolderList_PreviewKeyDown">
                    <TreeView.Resources>
                        <HierarchicalDataTemplate DataType="{x:Type models:FolderTreeNode}"
                                                  ItemsSource="{Binding Children}">
                            <!-- Visual display only. The TreeViewItem container carries the
                                 accessible name (AutomationProperties.Name → AutomationName)
                                 and unread count (AutomationProperties.ItemStatus →
                                 ItemStatusLabel). Splitting name and count across two UIA
                                 properties prevents JAWS from announcing the count twice
                                 (once per automation peer). -->
                            <StackPanel Orientation="Horizontal">
                                <local:AriaHiddenTextBlock TextTrimming="CharacterEllipsis">
                                    <local:AriaHiddenTextBlock.Style>
                                        <Style TargetType="local:AriaHiddenTextBlock">
                                            <Setter Property="Text" Value="{Binding Label}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsHeader}" Value="True">
                                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </local:AriaHiddenTextBlock.Style>
                                </local:AriaHiddenTextBlock>
                                <!-- Unread badge: "(5)" — collapsed when empty -->
                                <local:AriaHiddenTextBlock Margin="4,0,0,0"
                                                           Foreground="{DynamicResource Theme.TextSecondary}"
                                                           FontSize="{DynamicResource Theme.FontSizeSmall}"
                                                           Text="{Binding UnreadDisplay}">
                                    <local:AriaHiddenTextBlock.Style>
                                        <Style TargetType="local:AriaHiddenTextBlock">
                                            <Setter Property="Visibility" Value="Visible"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding UnreadDisplay}" Value="">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </local:AriaHiddenTextBlock.Style>
                                </local:AriaHiddenTextBlock>
                            </StackPanel>
                        </HierarchicalDataTemplate>
                    </TreeView.Resources>
                    <TreeView.ItemContainerStyle>
                        <Style TargetType="TreeViewItem" BasedOn="{StaticResource {x:Type TreeViewItem}}">
                            <Setter Property="IsExpanded"                  Value="{Binding IsExpanded, Mode=TwoWay}"/>
                            <Setter Property="AutomationProperties.Name"   Value="{Binding AutomationName}"/>
                            <Setter Property="AutomationProperties.ItemStatus" Value="{Binding ItemStatusLabel}"/>
                            <!-- Tag exposes the window DataContext (MainViewModel) to the ContextMenu -->
                            <Setter Property="Tag" Value="{Binding DataContext, RelativeSource={RelativeSource AncestorType=Window}}"/>
                            <Setter Property="ContextMenu" Value="{StaticResource FolderContextMenu}"/>
                        </Style>
                    </TreeView.ItemContainerStyle>
                </TreeView>
            </Grid>

            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch"
                          IsTabStop="False"
                          AutomationProperties.Name="{x:Static strings:Strings.MainWindow_ResizePanesName}"/>

            <!-- Right pane: messages + body (body docked to bottom, hidden until Enter on a message) -->
            <Grid Grid.Column="2">
            <DockPanel>

                <!-- Tab strip — visible only when message tabs are open -->
                <Border DockPanel.Dock="Top"
                        x:Name="TabStripBorder"
                        BorderThickness="0,0,0,1"
                        BorderBrush="{DynamicResource Theme.Border}"
                        Background="{DynamicResource Theme.ChromeBackground}"
                        Visibility="{Binding ShowTabStrip, Converter={StaticResource BoolToVisibilityConverter}}">
                    <ListBox x:Name="TabStrip"
                             ItemsSource="{Binding OpenTabs}"
                             SelectedItem="{Binding ActiveTab, Mode=TwoWay}"
                             SelectionMode="Single"
                             BorderThickness="0"
                             Background="Transparent"
                             ScrollViewer.HorizontalScrollBarVisibility="Auto"
                             ScrollViewer.VerticalScrollBarVisibility="Disabled"
                             KeyboardNavigation.TabNavigation="Once"
                             AutomationProperties.Name="{x:Static strings:Strings.MainWindow_OpenMessageTabsName}"
                             SelectionChanged="TabStrip_SelectionChanged"
                             PreviewKeyDown="TabStrip_PreviewKeyDown"
                             PreviewMouseDown="TabStrip_PreviewMouseDown"
                             GotKeyboardFocus="TabStrip_GotKeyboardFocus"
                             LostKeyboardFocus="TabStrip_LostKeyboardFocus">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="8,5"/>
                                <Setter Property="Margin" Value="0,0,1,0"/>
                                <Setter Property="AutomationProperties.Name" Value="{Binding Title}"/>
                                <Setter Property="ToolTip" Value="{Binding Title}"/>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Title}"
                                               MaxWidth="160"
                                               TextTrimming="CharacterEllipsis"
                                               VerticalAlignment="Center"/>
                                    <Button Content="✕"
                                            Width="16" Height="16"
                                            Padding="0"
                                            Margin="6,0,0,0"
                                            FontSize="9"
                                            Click="TabCloseButton_Click"
                                            Tag="{Binding}"
                                            IsTabStop="False"
                                            AutomationProperties.Name="{x:Static strings:Strings.MainWindow_CloseThisTabName}"
                                            ToolTip="{x:Static strings:Strings.MainWindow_CloseTabToolTip}"
                                            Visibility="{Binding CanClose, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Border>

                <!-- Body pane — hidden until IsMessageOpen; Escape returns to MessageList -->
                <Border DockPanel.Dock="Bottom"
                        BorderThickness="0,1,0,0"
                        BorderBrush="{DynamicResource Theme.Border}"
                        MinHeight="200"
                        Visibility="{Binding IsMessageOpen, Converter={StaticResource BoolToVisibilityConverter}}">
                    <DockPanel>
                        <StackPanel DockPanel.Dock="Top" Margin="8,6,8,0">
                            <TextBox x:Name="SubjectField"
                                     Text="{Binding MessageDetail.Subject, FallbackValue='', Mode=OneWay}"
                                     IsReadOnly="True" IsReadOnlyCaretVisible="True"
                                     BorderThickness="0" Background="Transparent"
                                     FontSize="{DynamicResource Theme.FontSizeBase}" FontWeight="SemiBold" TextWrapping="Wrap"
                                     AutomationProperties.Name="{x:Static strings:Strings.MainWindow_SubjectName}"/>
                            <TextBox x:Name="FromField"
                                     Text="{Binding MessageDetail.From, FallbackValue='', Mode=OneWay}"
                                     IsReadOnly="True" IsReadOnlyCaretVisible="True"
                                     BorderThickness="0" Background="Transparent"
                                     Margin="0,2,0,0"
                                     AutomationProperties.Name="{x:Static strings:Strings.MainWindow_From}"/>
                            <TextBox x:Name="ToField"
                                     Text="{Binding MessageDetail.To, FallbackValue='', Mode=OneWay}"
                                     IsReadOnly="True" IsReadOnlyCaretVisible="True"
                                     BorderThickness="0" Background="Transparent"
                                     AutomationProperties.Name="{x:Static strings:Strings.MainWindow_To}"/>
                            <TextBox x:Name="CcField"
                                     Text="{Binding MessageDetail.Cc, FallbackValue='', Mode=OneWay}"
                                     IsReadOnly="True" IsReadOnlyCaretVisible="True"
                                     BorderThickness="0" Background="Transparent"
                                     AutomationProperties.Name="{x:Static strings:Strings.MainWindow_CcName}">
                                <TextBox.Style>
                                    <Style TargetType="TextBox">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding MessageDetail.Cc}" Value="">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding MessageDetail.Cc}" Value="{x:Null}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBox.Style>
                            </TextBox>
                            <TextBox x:Name="DateField"
                                     Text="{Binding MessageDetail.Date, StringFormat='{}{0:f}', FallbackValue='', Mode=OneWay}"
                                     IsReadOnly="True" IsReadOnlyCaretVisible="True"
                                     BorderThickness="0" Background="Transparent"
                                     AutomationProperties.Name="{x:Static strings:Strings.MainWindow_DateName}"/>

                            <!-- Attachments (visible when selected message has attachments) -->
                            <ListBox x:Name="ReadingPaneAttachmentList"
                                     ItemsSource="{Binding MessageDetail.Attachments}"
                                     SelectionMode="Single"
                                     BorderThickness="0"
                                     Margin="0,4,0,0"
                                     Visibility="{Binding SelectedMessage.HasAttachments,
                                                  Converter={StaticResource BoolToVisibilityConverter},
                                                  FallbackValue=Collapsed}"
                                     KeyboardNavigation.TabNavigation="Once"
                                     KeyboardNavigation.DirectionalNavigation="Contained"
                                     AutomationProperties.Name="{x:Static strings:Strings.MainWindow_AttachmentsName}"
                                     PreviewKeyDown="ReadingPaneAttachmentList_PreviewKeyDown"
                                     GotKeyboardFocus="ReadingPaneAttachmentList_GotKeyboardFocus">
                                <ListBox.ItemContainerStyle>
                                    <Style TargetType="ListBoxItem">
                                        <Setter Property="AutomationProperties.Name" Value="{Binding AccessibleName}"/>
                                    </Style>
                                </ListBox.ItemContainerStyle>
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding FileName}" ToolTip="{Binding FileSizeDisplay}"/>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                                <ListBox.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Header="{x:Static strings:Strings.MainWindow_SaveEllipsis}"
                                                  Command="{Binding PlacementTarget.DataContext.SaveAttachmentCommand,
                                                            RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                  CommandParameter="{Binding PlacementTarget.SelectedItem,
                                                            RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                        <MenuItem Header="{x:Static strings:Strings.MainWindow_SaveAllEllipsis}"
                                                  Command="{Binding PlacementTarget.DataContext.SaveAllAttachmentsCommand,
                                                            RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                        <MenuItem Header="{x:Static strings:Strings.MainWindow_Open}"
                                                  Command="{Binding PlacementTarget.DataContext.OpenAttachmentCommand,
                                                            RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                  CommandParameter="{Binding PlacementTarget.SelectedItem,
                                                            RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                    </ContextMenu>
                                </ListBox.ContextMenu>
                            </ListBox>

                            <Separator Margin="0,4,0,0"/>
                        </StackPanel>
                        <!-- WebView2: real browser, proper arrow-key caret nav, screen-reader virtual cursor -->
                        <wv2:WebView2 x:Name="MessageBody"
                                      AutomationProperties.Name="{x:Static strings:Strings.MainWindow_MessageBodyName}"
                                      KeyboardNavigation.IsTabStop="True"
                                      GotKeyboardFocus="MessageBody_GotKeyboardFocus"
                                      MinHeight="80"/>
                    </DockPanel>
                </Border>

                <!-- Message list — fills all remaining space; body docked above takes its share -->
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top"
                               Text="{Binding SelectedFolder.DisplayName, FallbackValue={x:Static strings:Strings.MainWindow_NoFolderSelected}}"
                               FontWeight="SemiBold" Padding="6,4"
                               Focusable="False"
                               Background="{DynamicResource Theme.ChromeBackground}"
                               AutomationProperties.Name="{x:Static strings:Strings.MainWindow_CurrentFolderName}"/>

                    <!-- Search bar — visible only while IsSearchActive -->
                    <Border DockPanel.Dock="Top"
                            Padding="6,4"
                            BorderThickness="0,0,0,1"
                            BorderBrush="{DynamicResource Theme.Border}"
                            Visibility="{Binding IsSearchActive, Converter={StaticResource BoolToVisibilityConverter}}">
                        <DockPanel>
                            <Button DockPanel.Dock="Right"
                                    Content="{x:Static strings:Strings.MainWindow_ClearButton}"
                                    Command="{Binding ClearSearchCommand}"
                                    AutomationProperties.Name="{x:Static strings:Strings.MainWindow_ClearSearchName}"
                                    ToolTip="{x:Static strings:Strings.MainWindow_ClearSearchToolTip}"
                                    IsTabStop="True"/>
                            <TextBlock DockPanel.Dock="Right"
                                       VerticalAlignment="Center"
                                       Margin="6,0"
                                       Focusable="False"
                                       Text="{Binding Messages.Count, StringFormat='{}{0} found'}">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Visible"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding SearchText}" Value="">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <TextBox x:Name="SearchBox"
                                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                     AutomationProperties.Name="{x:Static strings:Strings.MainWindow_SearchMessagesName}"
                                     ToolTip="{x:Static strings:Strings.MainWindow_SearchBoxToolTip}"
                                     PreviewKeyDown="SearchBox_PreviewKeyDown"/>
                        </DockPanel>
                    </Border>

                    <TextBlock DockPanel.Dock="Top"
                               Text="{x:Static strings:Strings.MainWindow_NoMessages}"
                               HorizontalAlignment="Center" Margin="0,20">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Messages.Count}" Value="0">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <!-- Standard list view and conversation tree occupy the same space;
                         only one is visible at a time based on ViewMode. -->
                    <Grid>
                    <!-- Calendar event list — shown in place of the message list when the
                         Calendar virtual folder is selected. Behaves like the flat message
                         list: Up/Down to browse, Enter to open the source invite, T to
                         filter to today, F5 to refresh, Escape to return to the folder tree. -->
                    <ListBox x:Name="CalendarList"
                             AutomationProperties.Name="{x:Static strings:Strings.MainWindow_CalendarEventsName}"
                             Visibility="{Binding IsCalendarView, Converter={StaticResource BoolToVisibilityConverter}}"
                             ItemsSource="{Binding CalendarVm.Events}"
                             SelectedItem="{Binding CalendarVm.SelectedEvent, Mode=TwoWay}"
                             BorderThickness="0"
                             TabIndex="3"
                             KeyboardNavigation.TabNavigation="Once"
                             KeyboardNavigation.DirectionalNavigation="Contained"
                             VirtualizingPanel.IsVirtualizing="True"
                             VirtualizingPanel.VirtualizationMode="Recycling"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                             GotKeyboardFocus="CalendarList_GotKeyboardFocus"
                             PreviewKeyDown="CalendarList_PreviewKeyDown">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                                <Setter Property="AutomationProperties.Name" Value="{Binding DisplayLine}"/>
                                <Setter Property="Padding" Value="8,4"/>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding Summary}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding DisplayLine}" FontSize="{DynamicResource Theme.FontSizeSmall}" Foreground="{DynamicResource Theme.TextSecondary}" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <ListView x:Name="MessageList"
                              AutomationProperties.Name="{x:Static strings:Strings.MainWindow_MessagesName}"
                              ContextMenu="{StaticResource MessageContextMenu}"
                              ItemsSource="{Binding Messages}"
                              SelectedItem="{Binding SelectedMessage, Mode=TwoWay}"
                              SelectionMode="Extended"
                              BorderThickness="0"
                              TabIndex="3"
                              KeyboardNavigation.TabNavigation="Once"
                              KeyboardNavigation.DirectionalNavigation="Contained"
                              GotKeyboardFocus="List_GotKeyboardFocus"
                              PreviewTextInput="MessageList_PreviewTextInput"
                              PreviewKeyDown="MessageList_PreviewKeyDown"
                              MouseLeftButtonUp="MessageList_MouseLeftButtonUp">
                        <ListView.Style>
                            <Style TargetType="ListView" BasedOn="{StaticResource {x:Type ListView}}">
                                <Setter Property="Visibility" Value="{Binding IsMessagesView, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsCalendarView}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ListView.Style>
                        <!-- Column headers are mouse-clickable but not keyboard tab stops:
                             Focusable=False comes from the app-wide GridViewColumnHeader styles
                             (AccessibleStyles + ThemedControls), so the themed header template
                             is not shadowed by a local style here. -->
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Width="{Binding ShowMessageStatus, Converter={StaticResource BoolToColumnWidthConverter}}">
                                    <GridViewColumn.Header>
                                        <TextBlock Text="{x:Static strings:Strings.MainWindow_Status}"
                                                   AutomationProperties.IsColumnHeader="True"
                                                   Visibility="{Binding DataContext.ShowMessageStatus,
                                                       RelativeSource={RelativeSource AncestorType=Window},
                                                       Converter={StaticResource BoolToVisibilityConverter}}"/>
                                    </GridViewColumn.Header>
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding StatusDisplay}"
                                                       FontSize="{DynamicResource Theme.FontSizeSmall}"
                                                       TextAlignment="Center"
                                                       TextTrimming="CharacterEllipsis"
                                                       Visibility="{Binding DataContext.ShowMessageStatus,
                                                           RelativeSource={RelativeSource AncestorType=Window},
                                                           Converter={StaticResource BoolToVisibilityConverter}}"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Width="180">
                                    <GridViewColumn.Header>
                                        <GridViewColumnHeader Content="{x:Static strings:Strings.MainWindow_From}"
                                                              AutomationProperties.IsColumnHeader="True"/>
                                    </GridViewColumn.Header>
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding From}"
                                                       FontWeight="{Binding IsRead, Converter={StaticResource BoolToWeightConverter}}"
                                                       TextTrimming="CharacterEllipsis"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Width="Auto">
                                    <GridViewColumn.Header>
                                        <GridViewColumnHeader Content="{x:Static strings:Strings.MainWindow_Subject}"
                                                              AutomationProperties.IsColumnHeader="True"/>
                                    </GridViewColumn.Header>
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Subject}"
                                                       FontWeight="{Binding IsRead, Converter={StaticResource BoolToWeightConverter}}"
                                                       TextTrimming="CharacterEllipsis"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Width="80">
                                    <GridViewColumn.Header>
                                        <GridViewColumnHeader Content="{x:Static strings:Strings.MainWindow_DateName}"
                                                              AutomationProperties.IsColumnHeader="True"/>
                                    </GridViewColumn.Header>
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding DateDisplay}" TextAlignment="Right"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Width="24">
                                    <GridViewColumn.Header>
                                        <TextBlock Text="&#x1F4CE;" FontSize="{DynamicResource Theme.FontSizeSmall}"
                                                   ToolTip="{x:Static strings:Strings.MainWindow_HasAttachmentsToolTip}"
                                                   AutomationProperties.IsColumnHeader="True"/>
                                    </GridViewColumn.Header>
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="&#x1F4CE;" FontSize="{DynamicResource Theme.FontSizeSmall}" TextAlignment="Center">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding HasAttachments}" Value="True">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem" BasedOn="{StaticResource {x:Type ListViewItem}}">
                                <Setter Property="BorderThickness" Value="4,0,0,0"/>
                                <Setter Property="BorderBrush">
                                    <Setter.Value>
                                        <Binding Path="FlagColorHex" Converter="{StaticResource StringToBrushConverter}"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="AutomationProperties.Name">
                                    <Setter.Value>
                                        <MultiBinding Converter="{StaticResource MessageAccessibleNameConverter}">
                                            <Binding Path="FlagLabel"/>
                                            <Binding Path="ReadStatusLabel"/>
                                            <Binding Path="From"/>
                                            <Binding Path="Subject"/>
                                            <Binding Path="Preview"/>
                                            <Binding Path="DateDisplay"/>
                                            <Binding Path="DataContext.AnnounceFlagStatus"
                                                     RelativeSource="{RelativeSource AncestorType=Window}"/>
                                            <Binding Path="HasAttachments"/>
                                        </MultiBinding>
                                    </Setter.Value>
                                </Setter>
                                <!-- Token-driven row template. The default GridView container paints
                                     selection with hardcoded light colors that are unreadable on a
                                     dark theme; under High Contrast the token brushes resolve to the
                                     live SystemColors palette, so this stays HC-correct. The outer
                                     Border keeps the flag color bar (BorderBrush/Thickness above);
                                     the 3px inner bar is the additive unread cue — SemiBold text
                                     remains the primary cue. -->
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListViewItem">
                                            <Border BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    SnapsToDevicePixels="True">
                                                <Border x:Name="rowBg" Background="Transparent" Padding="2,1">
                                                    <DockPanel>
                                                        <Rectangle x:Name="unreadBar" DockPanel.Dock="Left"
                                                                   Width="3" Margin="0,1,3,1"
                                                                   Fill="{DynamicResource Theme.Accent}"
                                                                   Visibility="Collapsed"/>
                                                        <GridViewRowPresenter Content="{TemplateBinding Content}"
                                                                              Columns="{TemplateBinding GridView.ColumnCollection}"/>
                                                    </DockPanel>
                                                </Border>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <DataTrigger Binding="{Binding IsRead}" Value="False">
                                                    <Setter TargetName="unreadBar" Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="rowBg" Property="Background" Value="{DynamicResource Theme.AccentSubtle}"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="rowBg" Property="Background" Value="{DynamicResource Theme.SelectionBackground}"/>
                                                    <Setter Property="Foreground" Value="{DynamicResource Theme.SelectionText}"/>
                                                </Trigger>
                                                <MultiTrigger>
                                                    <MultiTrigger.Conditions>
                                                        <Condition Property="IsSelected" Value="True"/>
                                                        <Condition Property="Selector.IsSelectionActive" Value="False"/>
                                                    </MultiTrigger.Conditions>
                                                    <Setter TargetName="rowBg" Property="Background" Value="{DynamicResource Theme.SelectionInactive}"/>
                                                    <Setter Property="Foreground" Value="{DynamicResource Theme.TextPrimary}"/>
                                                </MultiTrigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListView.ItemContainerStyle>
                    </ListView>

                    <!-- Conversation tree view: one root node per subject group, children are individual messages. -->
                    <TreeView x:Name="ConversationTree"
                              AutomationProperties.Name="{x:Static strings:Strings.MainWindow_MessagesName}"
                              ItemsSource="{Binding Conversations}"
                              BorderThickness="0"
                              TabIndex="3"
                              KeyboardNavigation.TabNavigation="Once"
                              KeyboardNavigation.DirectionalNavigation="Contained"
                              VirtualizingPanel.IsVirtualizing="True"
                              VirtualizingPanel.VirtualizationMode="Recycling"
                              ScrollViewer.CanContentScroll="True"
                              GotKeyboardFocus="ConversationTree_GotKeyboardFocus"
                              SelectedItemChanged="ConversationTree_SelectedItemChanged"
                              PreviewTextInput="ConversationTree_PreviewTextInput"
                              PreviewKeyDown="ConversationTree_PreviewKeyDown"
                              PreviewMouseRightButtonDown="ConversationTree_PreviewMouseRightButtonDown"
                              ContextMenuOpening="ConversationTree_ContextMenuOpening">
                        <TreeView.Style>
                            <Style TargetType="TreeView" BasedOn="{StaticResource {x:Type TreeView}}">
                                <Setter Property="Visibility" Value="{Binding IsConversationsView, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsCalendarView}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TreeView.Style>
                        <TreeView.Resources>
                            <!-- Style for level-2 message rows (children of a conversation node) -->
                            <Style x:Key="MessageTreeItemStyle" TargetType="TreeViewItem"
                                   BasedOn="{StaticResource {x:Type TreeViewItem}}">
                                <Setter Property="AutomationProperties.Name">
                                    <Setter.Value>
                                        <MultiBinding Converter="{StaticResource MessageAccessibleNameConverter}">
                                            <Binding Path="FlagLabel"/>
                                            <Binding Path="ReadStatusLabel"/>
                                            <Binding Path="From"/>
                                            <Binding Path="Subject"/>
                                            <Binding Path="Preview"/>
                                            <Binding Path="DateDisplay"/>
                                            <Binding Path="DataContext.AnnounceFlagStatus"
                                                     RelativeSource="{RelativeSource AncestorType=Window}"/>
                                            <Binding Path="HasAttachments"/>
                                        </MultiBinding>
                                    </Setter.Value>
                                </Setter>
                                <!-- ContextMenu is intentionally omitted: ConversationTree_ContextMenuOpening
                                     assigns MessageContextMenu at the tree level so PlacementTarget.DataContext
                                     resolves to MainViewModel instead of MailMessageSummary. -->
                            </Style>

                            <!-- Level 1: conversation group header with subject + count + preview -->
                            <HierarchicalDataTemplate DataType="{x:Type models:ConversationGroup}"
                                                      ItemsSource="{Binding Messages}"
                                                      ItemContainerStyle="{StaticResource MessageTreeItemStyle}">
                                <DockPanel LastChildFill="True">
                                    <Grid DockPanel.Dock="Left" Width="16">
                                        <Border Width="4" HorizontalAlignment="Left" IsHitTestVisible="False"
                                                Background="{Binding FlagColorHex, Converter={StaticResource StringToBrushConverter}}"/>
                                        <Border Background="Transparent"
                                                Focusable="False"
                                                Tag="{Binding}"
                                                MouseLeftButtonUp="FlagHitArea_MouseLeftButtonUp"/>
                                    </Grid>
                                    <StackPanel>
                                        <TextBlock TextTrimming="CharacterEllipsis"
                                                   FontWeight="{Binding HasUnread, Converter={StaticResource BoolToWeightConverter}}">
                                            <Run Text="{Binding Subject, Mode=OneWay}"/>
                                            <Run Text=" "/>
                                            <Run Text="{Binding Count, StringFormat=({0}), Mode=OneWay}"/>
                                        </TextBlock>
                                        <TextBlock Text="{Binding LastSenderName}"
                                                   FontSize="{DynamicResource Theme.FontSizeSmall}" Opacity="0.85"
                                                   TextTrimming="CharacterEllipsis">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding LastSenderName}" Value="">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <TextBlock Text="{Binding Preview}"
                                                   FontSize="{DynamicResource Theme.FontSizeSmall}" Opacity="0.7"
                                                   TextTrimming="CharacterEllipsis">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Preview}" Value="">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                </DockPanel>
                            </HierarchicalDataTemplate>

                            <!-- Level 2: individual message row matching the ListView column layout -->
                            <DataTemplate DataType="{x:Type models:MailMessageSummary}">
                                <DockPanel LastChildFill="True">
                                    <Grid DockPanel.Dock="Left" Width="16">
                                        <Border Width="4" HorizontalAlignment="Left" IsHitTestVisible="False"
                                                Background="{Binding FlagColorHex, Converter={StaticResource StringToBrushConverter}}"/>
                                        <Border Background="Transparent"
                                                Focusable="False"
                                                Tag="{Binding}"
                                                MouseLeftButtonUp="FlagHitArea_MouseLeftButtonUp"/>
                                    </Grid>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="180"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="80"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding From}"
                                                   FontWeight="{Binding IsRead, Converter={StaticResource BoolToWeightConverter}}"
                                                   TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Subject}"
                                                   FontWeight="{Binding IsRead, Converter={StaticResource BoolToWeightConverter}}"
                                                   TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Grid.Column="2" Text="{Binding DateDisplay}"
                                                   TextAlignment="Right"/>
                                    </Grid>
                                </DockPanel>
                            </DataTemplate>
                        </TreeView.Resources>
                        <TreeView.ItemContainerStyle>
                            <!-- Style for root-level conversation group nodes: starts collapsed -->
                            <Style TargetType="TreeViewItem" BasedOn="{StaticResource {x:Type TreeViewItem}}">
                                <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                                <Setter Property="AutomationProperties.Name" Value="{Binding AutomationName}"/>
                            </Style>
                        </TreeView.ItemContainerStyle>
                    </TreeView>

                    <!-- From view: one root node per sender, children are individual messages. -->
                    <TreeView x:Name="SenderGroupTree"
                              AutomationProperties.Name="{x:Static strings:Strings.MainWindow_MessagesName}"
                              ItemsSource="{Binding SenderGroups}"
                              BorderThickness="0"
                              TabIndex="3"
                              KeyboardNavigation.TabNavigation="Once"
                              KeyboardNavigation.DirectionalNavigation="Contained"
                              VirtualizingPanel.IsVirtualizing="True"
                              VirtualizingPanel.VirtualizationMode="Recycling"
                              ScrollViewer.CanContentScroll="True"
                              GotKeyboardFocus="SenderGroupTree_GotKeyboardFocus"
                              SelectedItemChanged="SenderGroupTree_SelectedItemChanged"
                              PreviewTextInput="SenderGroupTree_PreviewTextInput"
                              PreviewKeyDown="SenderGroupTree_PreviewKeyDown"
                              PreviewMouseRightButtonDown="SenderGroupTree_PreviewMouseRightButtonDown"
                              ContextMenuOpening="SenderGroupTree_ContextMenuOpening">
                        <TreeView.Style>
                            <Style TargetType="TreeView" BasedOn="{StaticResource {x:Type TreeView}}">
                                <Setter Property="Visibility" Value="{Binding IsFromView, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsCalendarView}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TreeView.Style>
                        <TreeView.Resources>
                            <!-- Style for level-2 message rows (children of a sender group node) -->
                            <Style x:Key="SenderMessageTreeItemStyle" TargetType="TreeViewItem"
                                   BasedOn="{StaticResource {x:Type TreeViewItem}}">
                                <Setter Property="AutomationProperties.Name">
                                    <Setter.Value>
                                        <MultiBinding Converter="{StaticResource MessageAccessibleNameConverter}">
                                            <Binding Path="FlagLabel"/>
                                            <Binding Path="ReadStatusLabel"/>
                                            <Binding Path="From"/>
                                            <Binding Path="Subject"/>
                                            <Binding Path="Preview"/>
                                            <Binding Path="DateDisplay"/>
                                            <Binding Path="DataContext.AnnounceFlagStatus"
                                                     RelativeSource="{RelativeSource AncestorType=Window}"/>
                                            <Binding Path="HasAttachments"/>
                                        </MultiBinding>
                                    </Setter.Value>
                                </Setter>
                                <!-- ContextMenu is intentionally omitted here: SenderGroupTree_ContextMenuOpening
                                     assigns MessageContextMenu at the tree level so PlacementTarget.DataContext
                                     resolves to MainViewModel instead of MailMessageSummary. -->
                            </Style>

                            <!-- Level 1: sender group header with name + count + newest subject -->
                            <HierarchicalDataTemplate DataType="{x:Type models:SenderGroup}"
                                                      ItemsSource="{Binding Messages}"
                                                      ItemContainerStyle="{StaticResource SenderMessageTreeItemStyle}">
                                <DockPanel LastChildFill="True">
                                    <Grid DockPanel.Dock="Left" Width="16">
                                        <Border Width="4" HorizontalAlignment="Left" IsHitTestVisible="False"
                                                Background="{Binding FlagColorHex, Converter={StaticResource StringToBrushConverter}}"/>
                                        <Border Background="Transparent"
                                                Focusable="False"
                                                Tag="{Binding}"
                                                MouseLeftButtonUp="FlagHitArea_MouseLeftButtonUp"/>
                                    </Grid>
                                    <StackPanel>
                                        <TextBlock TextTrimming="CharacterEllipsis"
                                                   FontWeight="{Binding HasUnread, Converter={StaticResource BoolToWeightConverter}}">
                                            <Run Text="{Binding SenderName, Mode=OneWay}"/>
                                            <Run Text=" "/>
                                            <Run Text="{Binding Count, StringFormat=({0}), Mode=OneWay}"/>
                                        </TextBlock>
                                        <TextBlock Text="{Binding NewestSubject}"
                                                   FontSize="{DynamicResource Theme.FontSizeSmall}" Opacity="0.85"
                                                   TextTrimming="CharacterEllipsis">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding NewestSubject}" Value="">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <TextBlock Text="{Binding Preview}"
                                                   FontSize="{DynamicResource Theme.FontSizeSmall}" Opacity="0.7"
                                                   TextTrimming="CharacterEllipsis">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Preview}" Value="">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                </DockPanel>
                            </HierarchicalDataTemplate>

                            <!-- Level 2: individual message row matching the ListView column layout -->
                            <DataTemplate DataType="{x:Type models:MailMessageSummary}">
                                <DockPanel LastChildFill="True">
                                    <Grid DockPanel.Dock="Left" Width="16">
                                        <Border Width="4" HorizontalAlignment="Left" IsHitTestVisible="False"
                                                Background="{Binding FlagColorHex, Converter={StaticResource StringToBrushConverter}}"/>
                                        <Border Background="Transparent"
                                                Focusable="False"
                                                Tag="{Binding}"
                                                MouseLeftButtonUp="FlagHitArea_MouseLeftButtonUp"/>
                                    </Grid>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="180"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="80"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding From}"
                                                   FontWeight="{Binding IsRead, Converter={StaticResource BoolToWeightConverter}}"
                                                   TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Subject}"
                                                   FontWeight="{Binding IsRead, Converter={StaticResource BoolToWeightConverter}}"
                                                   TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Grid.Column="2" Text="{Binding DateDisplay}"
                                                   TextAlignment="Right"/>
                                    </Grid>
                                </DockPanel>
                            </DataTemplate>
                        </TreeView.Resources>
                        <TreeView.ItemContainerStyle>
                            <!-- Style for root-level sender group nodes: starts collapsed -->
                            <Style TargetType="TreeViewItem" BasedOn="{StaticResource {x:Type TreeViewItem}}">
                                <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                                <Setter Property="AutomationProperties.Name" Value="{Binding AutomationName}"/>
                            </Style>
                        </TreeView.ItemContainerStyle>
                    </TreeView>

                    <!-- To view: one root node per recipient, children are individual messages. -->
                    <TreeView x:Name="ToGroupTree"
                              AutomationProperties.Name="{x:Static strings:Strings.MainWindow_MessagesName}"
                              ItemsSource="{Binding ToGroups}"
                              BorderThickness="0"
                              TabIndex="3"
                              KeyboardNavigation.TabNavigation="Once"
                              KeyboardNavigation.DirectionalNavigation="Contained"
                              VirtualizingPanel.IsVirtualizing="True"
                              VirtualizingPanel.VirtualizationMode="Recycling"
                              ScrollViewer.CanContentScroll="True"
                              GotKeyboardFocus="ToGroupTree_GotKeyboardFocus"
                              SelectedItemChanged="ToGroupTree_SelectedItemChanged"
                              PreviewTextInput="ToGroupTree_PreviewTextInput"
                              PreviewKeyDown="ToGroupTree_PreviewKeyDown"
                              PreviewMouseRightButtonDown="ToGroupTree_PreviewMouseRightButtonDown"
                              ContextMenuOpening="ToGroupTree_ContextMenuOpening">
                        <TreeView.Style>
                            <Style TargetType="TreeView" BasedOn="{StaticResource {x:Type TreeView}}">
                                <Setter Property="Visibility" Value="{Binding IsToView, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsCalendarView}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TreeView.Style>
                        <TreeView.Resources>
                            <!-- Style for level-2 message rows (children of a recipient group node) -->
                            <Style x:Key="ToMessageTreeItemStyle" TargetType="TreeViewItem"
                                   BasedOn="{StaticResource {x:Type TreeViewItem}}">
                                <Setter Property="AutomationProperties.Name">
                                    <Setter.Value>
                                        <MultiBinding Converter="{StaticResource MessageAccessibleNameConverter}">
                                            <Binding Path="FlagLabel"/>
                                            <Binding Path="ReadStatusLabel"/>
                                            <Binding Path="From"/>
                                            <Binding Path="Subject"/>
                                            <Binding Path="Preview"/>
                                            <Binding Path="DateDisplay"/>
                                            <Binding Path="DataContext.AnnounceFlagStatus"
                                                     RelativeSource="{RelativeSource AncestorType=Window}"/>
                                            <Binding Path="HasAttachments"/>
                                        </MultiBinding>
                                    </Setter.Value>
                                </Setter>
                            </Style>

                            <!-- Level 1: recipient group header with address + count + newest subject -->
                            <HierarchicalDataTemplate DataType="{x:Type models:SenderGroup}"
                                                      ItemsSource="{Binding Messages}"
                                                      ItemContainerStyle="{StaticResource ToMessageTreeItemStyle}">
                                <DockPanel LastChildFill="True">
                                    <Grid DockPanel.Dock="Left" Width="16">
                                        <Border Width="4" HorizontalAlignment="Left" IsHitTestVisible="False"
                                                Background="{Binding FlagColorHex, Converter={StaticResource StringToBrushConverter}}"/>
                                        <Border Background="Transparent"
                                                Focusable="False"
                                                Tag="{Binding}"
                                                MouseLeftButtonUp="FlagHitArea_MouseLeftButtonUp"/>
                                    </Grid>
                                    <StackPanel>
                                        <TextBlock TextTrimming="CharacterEllipsis"
                                                   FontWeight="{Binding HasUnread, Converter={StaticResource BoolToWeightConverter}}">
                                            <Run Text="{Binding SenderName, Mode=OneWay}"/>
                                            <Run Text=" "/>
                                            <Run Text="{Binding Count, StringFormat=({0}), Mode=OneWay}"/>
                                        </TextBlock>
                                        <TextBlock Text="{Binding NewestSubject}"
                                                   FontSize="{DynamicResource Theme.FontSizeSmall}" Opacity="0.85"
                                                   TextTrimming="CharacterEllipsis">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding NewestSubject}" Value="">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <TextBlock Text="{Binding Preview}"
                                                   FontSize="{DynamicResource Theme.FontSizeSmall}" Opacity="0.7"
                                                   TextTrimming="CharacterEllipsis">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Preview}" Value="">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                </DockPanel>
                            </HierarchicalDataTemplate>

                            <!-- Level 2: individual message row matching the ListView column layout -->
                            <DataTemplate DataType="{x:Type models:MailMessageSummary}">
                                <DockPanel LastChildFill="True">
                                    <Grid DockPanel.Dock="Left" Width="16">
                                        <Border Width="4" HorizontalAlignment="Left" IsHitTestVisible="False"
                                                Background="{Binding FlagColorHex, Converter={StaticResource StringToBrushConverter}}"/>
                                        <Border Background="Transparent"
                                                Focusable="False"
                                                Tag="{Binding}"
                                                MouseLeftButtonUp="FlagHitArea_MouseLeftButtonUp"/>
                                    </Grid>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="180"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="80"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding From}"
                                                   FontWeight="{Binding IsRead, Converter={StaticResource BoolToWeightConverter}}"
                                                   TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Subject}"
                                                   FontWeight="{Binding IsRead, Converter={StaticResource BoolToWeightConverter}}"
                                                   TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Grid.Column="2" Text="{Binding DateDisplay}"
                                                   TextAlignment="Right"/>
                                    </Grid>
                                </DockPanel>
                            </DataTemplate>
                        </TreeView.Resources>
                        <TreeView.ItemContainerStyle>
                            <!-- Style for root-level recipient group nodes: starts collapsed -->
                            <Style TargetType="TreeViewItem" BasedOn="{StaticResource {x:Type TreeViewItem}}">
                                <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                                <Setter Property="AutomationProperties.Name" Value="{Binding AutomationName}"/>
                            </Style>
                        </TreeView.ItemContainerStyle>
                    </TreeView>

                    </Grid>
                </DockPanel>

            </DockPanel>

            </Grid>
        </Grid>

        <!-- ── Status bar ── -->
        <!-- Each StatusBarItem is a distinct focus stop within the status bar.
             Left/Right arrows navigate between regions; Tab exits to the next F6 pane.
             Informational regions use read-only TextBox (ControlType.Edit + ValuePattern).
             Clickable regions use Button (ControlType.Button + InvokePattern).
             The StatusBar UIA control pattern enables screen readers' built-in
             status-bar read commands. -->
        <StatusBar x:Name="MainStatusBar"
                   Grid.Row="3"
                   KeyboardNavigation.TabNavigation="None"
                   KeyboardNavigation.DirectionalNavigation="Contained"
                   AutomationProperties.Name="">

            <!-- Region 1: Status message + Last sync time (single TextBlock for unified screen reader reading).
                 E.g., "7323 messages. Synced 9:29 AM" or "Connecting and syncing... Never synced" -->
            <StatusBarItem x:Name="StatusTextItem"
                           KeyboardNavigation.IsTabStop="False">
                <TextBlock x:Name="StatusTextBox"
                           Style="{StaticResource StatusTextBlockStyle}">
                    <Run Text="{Binding StatusText, Mode=OneWay}"/>
                    <Run Text="{Binding LastSyncText, StringFormat=' {0}', Mode=OneWay}"/>
                </TextBlock>
            </StatusBarItem>

            <!-- Region 2: Connection state (informational) -->
            <StatusBarItem x:Name="ConnectionStatusItem"
                           KeyboardNavigation.IsTabStop="False">
                <TextBlock x:Name="ConnectionStatusTextBox"
                           Style="{StaticResource StatusTextBlockStyle}"
                           Text="{Binding ConnectionStatusText, Mode=OneWay}"/>
            </StatusBarItem>

            <!-- Spacer: pushes Rules and Progress to the right -->
            <Separator Style="{StaticResource StatusBarSpacerStyle}"/>

            <!-- Region 3: Update notification (hidden until a newer release is detected).
                 Visibility tracks UpdateReleaseUrl (set only when an update exists) rather than the
                 menu label, which is now always populated ("No updates available" at rest). -->
            <StatusBarItem x:Name="UpdateStatusItem"
                           HorizontalAlignment="Right"
                           KeyboardNavigation.IsTabStop="True"
                           Visibility="{Binding UpdateReleaseUrl, Converter={StaticResource StringToVisibilityConverter}}">
                <Button x:Name="UpdateStatusButton"
                        Style="{StaticResource StatusBarButtonStyle}"
                        Content="{Binding UpdateAvailableText, Mode=OneWay}"
                        Command="{Binding OpenUpdatePageCommand}"
                        ToolTip="{x:Static strings:Strings.MainWindow_UpdateStatusToolTip}"/>
            </StatusBarItem>

            <!-- Region 4: Rules status (clickable — opens Rules Manager) -->
            <StatusBarItem x:Name="RulesStatusItem"
                           HorizontalAlignment="Right"
                           KeyboardNavigation.IsTabStop="True">
                <Button x:Name="RulesStatusButton"
                        Style="{StaticResource StatusBarButtonStyle}"
                        Content="{Binding RulesStatusText, Mode=OneWay}"
                        Click="RulesStatusButton_Click"
                        AutomationProperties.Name="{Binding RulesStatusText, StringFormat={x:Static strings:Strings.MainWindow_RulesStatusNameFormat}}"
                        ToolTip="{x:Static strings:Strings.MainWindow_RulesStatusToolTip}"/>
            </StatusBarItem>

            <!-- Region 4: Sync progress (informational, visible only when busy) -->
            <StatusBarItem x:Name="StatusProgressItem"
                           HorizontalAlignment="Right"
                           KeyboardNavigation.IsTabStop="True"
                           Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}">
                <ProgressBar x:Name="StatusProgressBar"
                             Width="80" Height="14"
                             IsIndeterminate="True"
                             Focusable="True"
                             AutomationProperties.Name="{Binding StatusText, StringFormat={x:Static strings:Strings.MainWindow_SyncingStatusNameFormat}}"/>
            </StatusBarItem>
        </StatusBar>

        <!-- First-run keyboard tutorial overlay (topmost, covers everything when active) -->
        <local:TutorialOverlay x:Name="TutorialOverlayControl"
                               Grid.Row="0" Grid.RowSpan="4"
                               Panel.ZIndex="1000"/>
    </Grid>
</Window>
