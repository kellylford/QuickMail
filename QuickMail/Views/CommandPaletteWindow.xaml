<Window x:Class="QuickMail.Views.CommandPaletteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:QuickMail.ViewModels"
        mc:Ignorable="d"
        Title="Command Palette"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        Width="560"
        SizeToContent="Height"
        MaxHeight="420"
        WindowStartupLocation="Manual"
        ShowInTaskbar="False"
        d:DataContext="{d:DesignInstance Type=vm:CommandPaletteViewModel}"
        PreviewKeyDown="OnPreviewKeyDown">

    <Border Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"
            BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"
            BorderThickness="1"
            CornerRadius="4">
        <Border.Effect>
            <DropShadowEffect BlurRadius="12" ShadowDepth="3" Opacity="0.35" Direction="270"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Search box -->
            <TextBox x:Name="SearchBox"
                     Grid.Row="0"
                     Margin="8,8,8,4"
                     Padding="6,4"
                     FontSize="13"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     AutomationProperties.Name="Search commands"
                     AutomationProperties.HelpText="Type to filter commands. Use Up/Down arrows to select, Enter to run, Escape to close."
                     PreviewKeyDown="SearchBox_PreviewKeyDown"/>

            <!-- Separator -->
            <Separator Grid.Row="0" Margin="8,0" VerticalAlignment="Bottom"/>

            <!-- Command list -->
            <ListBox x:Name="CommandList"
                     Grid.Row="1"
                     Margin="4,4,4,4"
                     MaxHeight="340"
                     BorderThickness="0"
                     Background="Transparent"
                     ItemsSource="{Binding FilteredCommands}"
                     SelectedItem="{Binding SelectedCommand}"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     VirtualizingPanel.IsVirtualizing="True"
                     VirtualizingPanel.VirtualizationMode="Recycling"
                     AutomationProperties.Name="Commands"
                     MouseDoubleClick="CommandList_MouseDoubleClick"
                     PreviewTextInput="CommandList_PreviewTextInput"
                     PreviewKeyDown="CommandList_PreviewKeyDown">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="8,5"/>
                        <Setter Property="IsTabStop" Value="False"/>
                        <Setter Property="AutomationProperties.Name"
                                Value="{Binding Title}"/>
                        <Setter Property="AutomationProperties.HelpText"
                                Value="{Binding GestureText}"/>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Title}"
                                           FontSize="13"
                                           TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{Binding Category}"
                                           FontSize="11"
                                           Opacity="0.6"
                                           Margin="0,1,0,0"/>
                            </StackPanel>

                            <TextBlock Grid.Column="1"
                                       Text="{Binding GestureText}"
                                       FontSize="11"
                                       FontFamily="Consolas"
                                       Opacity="0.6"
                                       VerticalAlignment="Center"
                                       Margin="12,0,0,0"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Border>
</Window>
