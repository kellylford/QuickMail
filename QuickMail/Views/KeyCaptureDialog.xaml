<Window x:Class="QuickMail.Views.KeyCaptureDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:strings="clr-namespace:QuickMail.Resources"
        mc:Ignorable="d"
        Title="{x:Static strings:Strings.KeyCapture_Title}"
        Height="220" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        ShowInTaskbar="False"
        Loaded="Window_Loaded"
        PreviewKeyDown="Window_PreviewKeyDown"
        AutomationProperties.Name="{x:Static strings:Strings.KeyCapture_DialogName}"
        Background="{DynamicResource Theme.WindowBackground}"
        Foreground="{DynamicResource Theme.TextPrimary}"
        FontFamily="{DynamicResource Theme.FontFamily}"
        FontSize="{DynamicResource Theme.FontSizeBase}">

    <Grid Margin="20" Focusable="True" x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Listening-state instructions -->
        <TextBlock x:Name="ListeningHeader"
                   Grid.Row="0"
                   Text="{x:Static strings:Strings.KeyCapture_PressDesiredCombination}"
                   FontSize="{DynamicResource Theme.FontSizeLarge}"
                   FontWeight="SemiBold"
                   Margin="0,0,0,4"/>
        <TextBlock x:Name="ListeningHint"
                   Grid.Row="1"
                   Text="{x:Static strings:Strings.KeyCapture_ModifierHint}"
                   Foreground="{DynamicResource Theme.TextSecondary}"
                   Margin="0,0,0,16"/>

        <!-- Captured key display -->
        <Border Grid.Row="2"
                BorderThickness="1"
                BorderBrush="{DynamicResource Theme.Border}"
                Background="{DynamicResource Theme.SurfaceBackground}"
                Padding="12"
                Margin="0,0,0,8"
                CornerRadius="2">
            <TextBlock x:Name="CapturedKeyText"
                       Text="{x:Static strings:Strings.KeyCapture_WaitingForInput}"
                       Foreground="{DynamicResource Theme.TextSecondary}"
                       TextAlignment="Center"
                       AutomationProperties.Name="{x:Static strings:Strings.KeyCapture_CapturedKeyCombination}"/>
        </Border>

        <!-- Conflict warning (shown only when the captured combo collides) -->
        <TextBlock x:Name="ConflictText"
                   Grid.Row="3"
                   Visibility="Collapsed"
                   Foreground="{DynamicResource Theme.Error}"
                   TextWrapping="Wrap"
                   Margin="0,0,0,8"
                   AutomationProperties.LiveSetting="Polite"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="5"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button x:Name="OkButton"
                    Content="{x:Static strings:Strings.Common_OK}"
                    IsDefault="True"
                    Click="OkButton_Click"
                    MinWidth="75"
                    Visibility="Collapsed"
                    Margin="0,0,8,0"
                    AutomationProperties.Name="{x:Static strings:Strings.KeyCapture_AcceptShortcut}"/>
            <Button x:Name="ChangeButton"
                    Content="{x:Static strings:Strings.KeyCapture_ChangeButtonContent}"
                    Click="ChangeButton_Click"
                    MinWidth="75"
                    Visibility="Collapsed"
                    Margin="0,0,8,0"
                    AutomationProperties.Name="{x:Static strings:Strings.KeyCapture_CaptureDifferentShortcut}"/>
            <Button Content="{x:Static strings:Strings.Common_Cancel}"
                    IsCancel="True"
                    MinWidth="75"
                    AutomationProperties.Name="{x:Static strings:Strings.KeyCapture_CancelClose}"/>
        </StackPanel>
    </Grid>
</Window>
