<Window x:Class="QuickMail.Views.KeyCaptureDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Assign Shortcut"
        Height="220" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        ShowInTaskbar="False"
        Loaded="Window_Loaded"
        PreviewKeyDown="Window_PreviewKeyDown"
        AutomationProperties.Name="Assign keyboard shortcut dialog">

    <Grid Margin="20" Focusable="True" x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Listening-state instructions -->
        <TextBlock x:Name="ListeningHeader"
                   Grid.Row="0"
                   Text="Press your desired key combination"
                   FontSize="14"
                   FontWeight="SemiBold"
                   Margin="0,0,0,4"/>
        <TextBlock x:Name="ListeningHint"
                   Grid.Row="1"
                   Text="(with Ctrl, Shift, or Alt)"
                   FontSize="12"
                   Foreground="DimGray"
                   Margin="0,0,0,16"/>

        <!-- Captured key display -->
        <Border Grid.Row="2"
                BorderThickness="1"
                BorderBrush="LightGray"
                Background="#F5F5F5"
                Padding="12"
                Margin="0,0,0,8"
                CornerRadius="2">
            <TextBlock x:Name="CapturedKeyText"
                       Text="(waiting for input...)"
                       FontSize="13"
                       Foreground="DimGray"
                       TextAlignment="Center"
                       AutomationProperties.Name="Captured key combination"/>
        </Border>

        <!-- Conflict warning (shown only when the captured combo collides) -->
        <TextBlock x:Name="ConflictText"
                   Grid.Row="3"
                   Visibility="Collapsed"
                   FontSize="12"
                   Foreground="#D32F2F"
                   TextWrapping="Wrap"
                   Margin="0,0,0,8"
                   AutomationProperties.LiveSetting="Polite"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="5"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button x:Name="OkButton"
                    Content="_OK"
                    IsDefault="True"
                    Click="OkButton_Click"
                    MinWidth="75"
                    Visibility="Collapsed"
                    Margin="0,0,8,0"
                    AutomationProperties.Name="Accept this shortcut"/>
            <Button x:Name="ChangeButton"
                    Content="C_hange"
                    Click="ChangeButton_Click"
                    MinWidth="75"
                    Visibility="Collapsed"
                    Margin="0,0,8,0"
                    AutomationProperties.Name="Capture a different shortcut"/>
            <Button Content="_Cancel"
                    IsCancel="True"
                    MinWidth="75"
                    AutomationProperties.Name="Cancel and close without saving"/>
        </StackPanel>
    </Grid>
</Window>
