<Window x:Class="QuickMail.Views.SettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Settings"
        Height="560" Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        Background="{DynamicResource Theme.WindowBackground}"
        Foreground="{DynamicResource Theme.TextPrimary}"
        FontFamily="{DynamicResource Theme.FontFamily}"
        FontSize="{DynamicResource Theme.FontSizeBase}"
        Loaded="Window_Loaded">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Tabs -->
        <TabControl Grid.Row="0" Margin="0,0,0,10">
            <!-- General Settings Tab -->
            <TabItem Header="_General" AutomationProperties.Name="General settings">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10" >
                        <!-- Message List Group -->
                        <GroupBox Header="Message _List" Padding="10" Margin="0,0,0,15" AutomationProperties.Name="Message list settings">
                            <StackPanel>
                                <!-- Preview Lines -->
                                <StackPanel>
                                    <Label x:Name="PreviewLinesLabel"
                                           Content="Preview _lines:"
                                           Target="{Binding ElementName=PreviewLinesCombo}"
                                           FontWeight="SemiBold"/>
                                    <ComboBox x:Name="PreviewLinesCombo"
                                              SelectedValue="{Binding PreviewLines, Mode=TwoWay}"
                                              SelectedValuePath="Tag"
                                              AutomationProperties.LabeledBy="{Binding ElementName=PreviewLinesLabel}">
                                        <ComboBoxItem Tag="0" Content="Off"/>
                                        <ComboBoxItem Tag="1" Content="1 line"/>
                                        <ComboBoxItem Tag="2" Content="2 lines"/>
                                        <ComboBoxItem Tag="3" Content="3 lines"/>
                                        <ComboBoxItem Tag="4" Content="4 lines"/>
                                        <ComboBoxItem Tag="5" Content="5 lines"/>
                                    </ComboBox>
                                    <TextBlock Text="Takes effect after reopening a folder."
                                               Foreground="{DynamicResource Theme.TextSecondary}"
                                               FontSize="{DynamicResource Theme.FontSizeSmall}"
                                               Margin="0,4,0,0"/>
                                </StackPanel>

                                <!-- Show Message Status -->
                                <CheckBox Content="Show message _status column"
                                          IsChecked="{Binding ShowMessageStatus, Mode=TwoWay}"
                                          Margin="0,8,0,0"
                                          AutomationProperties.Name="Show message status column in message list"/>

                                <!-- Read as Plain Text (issue #34) -->
                                <CheckBox Content="Read messages as _plain text"
                                          IsChecked="{Binding ReadAsPlainText, Mode=TwoWay}"
                                          Margin="0,8,0,0"
                                          AutomationProperties.Name="Read messages as plain text"/>
                            </StackPanel>
                        </GroupBox>

                        <!-- View Group -->
                        <GroupBox Header="_View" Padding="10" Margin="0,0,0,15" AutomationProperties.Name="View settings">
                            <StackPanel>
                                <!-- View Mode -->
                                <StackPanel>
                                    <Label x:Name="ViewModeLabel"
                                           Content="Display mode:"
                                           Target="{Binding ElementName=ViewModeCombo}"
                                           FontWeight="SemiBold"/>
                                    <ComboBox x:Name="ViewModeCombo"
                                              SelectedValue="{Binding ViewMode, Mode=TwoWay}"
                                              SelectedValuePath="Tag"
                                              AutomationProperties.LabeledBy="{Binding ElementName=ViewModeLabel}">
                                        <ComboBoxItem Tag="messages" Content="Messages (flat list)"/>
                                        <ComboBoxItem Tag="conversations" Content="Conversations (grouped by subject)"/>
                                        <ComboBoxItem Tag="from" Content="By Sender"/>
                                        <ComboBoxItem Tag="to" Content="By Recipient"/>
                                    </ComboBox>
                                </StackPanel>
                            </StackPanel>
                        </GroupBox>

                        <!-- Sync Group -->
                        <GroupBox Header="_Sync" Padding="10" AutomationProperties.Name="Synchronization settings">
                            <StackPanel>
                                <!-- Sync Days -->
                                <StackPanel>
                                    <Label x:Name="SyncDaysLabel" Margin="0,0,0,0"
                                           Content="Sync _range:"
                                           Target="{Binding ElementName=SyncDaysCombo}"
                                           FontWeight="SemiBold"/>
                                    <ComboBox x:Name="SyncDaysCombo"
                                              SelectedValue="{Binding SyncDays, Mode=TwoWay}"
                                              SelectedValuePath="Tag"
                                              AutomationProperties.LabeledBy="{Binding ElementName=SyncDaysLabel}">
                                        <ComboBoxItem Tag="7" Content="7 days"/>
                                        <ComboBoxItem Tag="30" Content="30 days"/>
                                        <ComboBoxItem Tag="180" Content="6 months"/>
                                        <ComboBoxItem Tag="365" Content="1 year"/>
                                        <ComboBoxItem Tag="0" Content="All mail"/>
                                    </ComboBox>
                                </StackPanel>

                                <!-- Initial Sync Count -->
                                <StackPanel Margin="0,8,0,0">
                                    <Label x:Name="InitialSyncLabel"
                                           Content="Initial fetch _count:"
                                           Target="{Binding ElementName=InitialSyncCombo}"
                                           FontWeight="SemiBold"/>
                                    <ComboBox x:Name="InitialSyncCombo"
                                              SelectedValue="{Binding InitialSyncCount, Mode=TwoWay}"
                                              SelectedValuePath="Tag"
                                              AutomationProperties.LabeledBy="{Binding ElementName=InitialSyncLabel}">
                                        <ComboBoxItem Tag="100" Content="100 messages"/>
                                        <ComboBoxItem Tag="250" Content="250 messages"/>
                                        <ComboBoxItem Tag="500" Content="500 messages"/>
                                        <ComboBoxItem Tag="1000" Content="1,000 messages"/>
                                        <ComboBoxItem Tag="0" Content="All messages"/>
                                    </ComboBox>
                                </StackPanel>
                            </StackPanel>
                        </GroupBox>

                        <!-- Mail Actions Group -->
                        <GroupBox Header="Mail _Actions" Padding="10" Margin="0,10,0,0"
                                  AutomationProperties.Name="Mail action settings">
                            <StackPanel>
                                <CheckBox Content="Confirm before emptying _trash"
                                          IsChecked="{Binding ConfirmEmptyTrash, Mode=TwoWay}"
                                          AutomationProperties.Name="Show a confirmation dialog before permanently deleting all messages in trash"/>
                                <TextBlock Text="Recommended: emptying trash cannot be undone."
                                           Foreground="{DynamicResource Theme.TextSecondary}" FontSize="{DynamicResource Theme.FontSizeSmall}" Margin="0,3,0,0"/>
                            </StackPanel>
                        </GroupBox>

                        <!-- Notifications Group -->
                        <GroupBox Header="_Notifications" Padding="10" Margin="0,10,0,0"
                                  AutomationProperties.Name="Notification settings">
                            <StackPanel>
                                <CheckBox Content="Show a _notification when new mail arrives"
                                          IsChecked="{Binding NotifyOnNewMail, Mode=TwoWay}"
                                          AutomationProperties.Name="Show a notification when new mail arrives"/>
                                <TextBlock Text="Windows shows a notification when new mail arrives in an inbox while QuickMail is running."
                                           Foreground="{DynamicResource Theme.TextSecondary}" FontSize="{DynamicResource Theme.FontSizeSmall}" Margin="0,3,0,8" TextWrapping="Wrap"/>

                                <CheckBox Content="_Keep running in the notification area when I close the window"
                                          IsChecked="{Binding CloseToTray, Mode=TwoWay}"
                                          AutomationProperties.Name="Keep running in the notification area when I close the window"/>
                                <TextBlock Text="Closing the window hides QuickMail to the notification area (system tray) instead of exiting, so notifications keep arriving. Restore it from the tray icon or a notification; use File then Exit, or the tray icon's Exit, to quit."
                                           Foreground="{DynamicResource Theme.TextSecondary}" FontSize="{DynamicResource Theme.FontSizeSmall}" Margin="0,3,0,0" TextWrapping="Wrap"/>
                            </StackPanel>
                        </GroupBox>

                        <!-- Desktop Shortcut Group -->
                        <GroupBox Header="Desktop Shortc_ut" Padding="10" Margin="0,10,0,0"
                                  AutomationProperties.Name="Desktop shortcut settings">
                            <StackPanel>
                                <CheckBox Content="Keep a QuickMail shortcut _on the desktop"
                                          IsChecked="{Binding DesktopShortcut, Mode=TwoWay}"
                                          AutomationProperties.Name="Desktop shortcut"/>
                                <TextBlock Text="The Start Menu entry is always available; this adds or removes the desktop shortcut. Applied when settings are saved."
                                           Foreground="{DynamicResource Theme.TextSecondary}" FontSize="{DynamicResource Theme.FontSizeSmall}" Margin="0,3,0,0" TextWrapping="Wrap"/>
                            </StackPanel>
                        </GroupBox>

                        <!-- Composing Group -->
                        <GroupBox Header="Com_posing" Padding="10" Margin="0,10,0,0"
                                  AutomationProperties.Name="Composing settings">
                            <StackPanel>
                                <CheckBox Content="Automatically save _drafts while composing"
                                          IsChecked="{Binding AutoSaveDrafts, Mode=TwoWay}"
                                          AutomationProperties.Name="Automatically save the message as a draft while composing"/>
                                <TextBlock Text="Auto-save is quiet: the time of the last save shows in the compose status row. A failed save is announced once."
                                           Foreground="{DynamicResource Theme.TextSecondary}" FontSize="{DynamicResource Theme.FontSizeSmall}" Margin="0,3,0,8" TextWrapping="Wrap"/>

                                <StackPanel Margin="0,0,0,8">
                                    <Label x:Name="AutoSaveIntervalLabel"
                                           Content="Auto-save _interval:"
                                           Target="{Binding ElementName=AutoSaveIntervalCombo}"
                                           FontWeight="SemiBold"/>
                                    <ComboBox x:Name="AutoSaveIntervalCombo"
                                              SelectedValue="{Binding AutoSaveIntervalSeconds, Mode=TwoWay}"
                                              SelectedValuePath="Tag"
                                              IsEnabled="{Binding AutoSaveDrafts}"
                                              AutomationProperties.LabeledBy="{Binding ElementName=AutoSaveIntervalLabel}">
                                        <ComboBoxItem Tag="30" Content="30 seconds"/>
                                        <ComboBoxItem Tag="60" Content="1 minute"/>
                                        <ComboBoxItem Tag="120" Content="2 minutes"/>
                                        <ComboBoxItem Tag="300" Content="5 minutes"/>
                                        <ComboBoxItem Tag="600" Content="10 minutes"/>
                                    </ComboBox>
                                </StackPanel>

                                <StackPanel>
                                    <Label x:Name="DefaultComposeModeLabel"
                                           Content="Default compose _mode:"
                                           Target="{Binding ElementName=DefaultComposeModeCombo}"
                                           FontWeight="SemiBold"/>
                                    <ComboBox x:Name="DefaultComposeModeCombo"
                                              SelectedValue="{Binding DefaultComposeMode, Mode=TwoWay}"
                                              SelectedValuePath="Tag"
                                              AutomationProperties.LabeledBy="{Binding ElementName=DefaultComposeModeLabel}">
                                        <ComboBoxItem Tag="plain" Content="Plain Text"/>
                                        <ComboBoxItem Tag="markdown" Content="Markdown"/>
                                        <ComboBoxItem Tag="html" Content="HTML"/>
                                    </ComboBox>
                                    <TextBlock Text="Drafts and templates always reopen in plain text."
                                               Foreground="{DynamicResource Theme.TextSecondary}" FontSize="{DynamicResource Theme.FontSizeSmall}" Margin="0,3,0,0"/>
                                </StackPanel>
                            </StackPanel>
                        </GroupBox>

                        <!-- Screen Reader Announcements Group -->
                        <GroupBox Header="Screen Reader _Announcements" Padding="10" Margin="0,10,0,0"
                                  AutomationProperties.Name="Screen reader announcement settings">
                            <StackPanel>
                                <CheckBox Content="Enable custom _announcements"
                                          IsChecked="{Binding CustomAnnouncements, Mode=TwoWay}"
                                          FontWeight="SemiBold"
                                          AutomationProperties.Name="Enable all custom screen reader announcements from QuickMail"/>
                                <TextBlock Text="When off, only native control announcements are spoken."
                                           Foreground="{DynamicResource Theme.TextSecondary}" FontSize="{DynamicResource Theme.FontSizeSmall}" Margin="0,2,0,8"/>

                                <CheckBox Content="Announce _hints and instructions"
                                          IsChecked="{Binding AnnounceHints, Mode=TwoWay}"
                                          Margin="16,0,0,0"
                                          AutomationProperties.Name="Announce instructional hints such as how to use the search box"/>

                                <CheckBox Content="Announce loading and sync _status"
                                          IsChecked="{Binding AnnounceStatus, Mode=TwoWay}"
                                          Margin="16,4,0,0"
                                          AutomationProperties.Name="Announce background loading and sync progress"/>

                                <CheckBox Content="Announce action _results"
                                          IsChecked="{Binding AnnounceResults, Mode=TwoWay}"
                                          Margin="16,4,0,0"
                                          AutomationProperties.Name="Announce outcomes such as search result counts and move confirmations"/>

                                <CheckBox Content="Announce spelling errors _when typing"
                                          IsChecked="{Binding AnnounceSpellingWhileTyping, Mode=TwoWay}"
                                          Margin="16,4,0,0"
                                          AutomationProperties.Name="Announce spelling errors when typing"/>

                                <CheckBox Content="Announce spelling errors while _navigating"
                                          IsChecked="{Binding AnnounceSpellingWhileNavigating, Mode=TwoWay}"
                                          Margin="16,4,0,0"
                                          AutomationProperties.Name="Announce spelling errors when the caret moves into a misspelled word during navigation"
                                          AutomationProperties.HelpText="Ctrl+F7 and Ctrl+Shift+F7 always announce spelling regardless of this setting"/>

                                <CheckBox Content="Announce spelling _suggestions"
                                          IsChecked="{Binding AnnounceSpellingSuggestions, Mode=TwoWay}"
                                          Margin="16,4,0,0"
                                          AutomationProperties.Name="Announce spelling suggestions when a misspelling is announced"/>

                                <TextBlock Text="Spelling Suggestions Verbosity"
                                           Margin="32,6,0,2"/>
                                <StackPanel KeyboardNavigation.TabNavigation="Once"
                                            KeyboardNavigation.DirectionalNavigation="Cycle"
                                            Margin="32,0,0,4">
                                    <RadioButton Content="_Numbers with suggestions"
                                                 IsChecked="{Binding IsVerbosityNumbersWithSuggestions, Mode=TwoWay}"
                                                 GroupName="SpellingSuggestionsVerbosity"
                                                 Checked="RadioButton_Checked"
                                                 AutomationProperties.Name="Numbers with suggestions"
                                                 AutomationProperties.HelpText="Each suggestion is preceded by its number, for example: 1: the, 2: then, 3: them"/>
                                    <RadioButton Content="_Just suggestions"
                                                 IsChecked="{Binding IsVerbosityJustSuggestions, Mode=TwoWay}"
                                                 GroupName="SpellingSuggestionsVerbosity"
                                                 Checked="RadioButton_Checked"
                                                 AutomationProperties.Name="Just suggestions"
                                                 AutomationProperties.HelpText="Suggestions are read without numbers, for example: the, then, them"/>
                                </StackPanel>

                                <CheckBox Content="Announce _formatting while navigating in HTML compose"
                                          IsChecked="{Binding AnnounceFormattingWhileNavigating, Mode=TwoWay}"
                                          Margin="16,4,0,0"
                                          AutomationProperties.Name="Announce block type such as heading level when the caret moves to a different paragraph in HTML compose mode"/>

                                <CheckBox Content="Announce fla_g status"
                                          IsChecked="{Binding AnnounceFlagStatus, Mode=TwoWay}"
                                          Margin="16,4,0,0"
                                          AutomationProperties.Name="Announce flag status"/>

                                <CheckBox Content="Show field _labels in the contact list"
                                          IsChecked="{Binding ContactListShowFieldLabels, Mode=TwoWay}"
                                          Margin="16,4,0,0"
                                          AutomationProperties.Name="Show field labels in the address book contact list accessible names"/>

                                <CheckBox Content="Show field labels in the _calendar event list"
                                          IsChecked="{Binding CalendarListShowFieldLabels, Mode=TwoWay}"
                                          Margin="16,4,0,0"
                                          AutomationProperties.Name="Show field labels in the calendar event list accessible names"/>

                                <CheckBox Content="Remind me before appoin_tments"
                                          IsChecked="{Binding CalendarReminders, Mode=TwoWay}"
                                          Margin="16,4,0,0"
                                          AutomationProperties.Name="Remind me before appointments"/>
                                <StackPanel Orientation="Horizontal" Margin="32,4,0,0"
                                            IsEnabled="{Binding CalendarReminders}">
                                    <TextBlock Text="Minutes before:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                    <TextBox Text="{Binding CalendarReminderMinutes, Mode=TwoWay}"
                                             AutomationProperties.Name="Reminder minutes before appointment"
                                             Width="48"/>
                                </StackPanel>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Advanced Tab -->
            <TabItem Header="_Advanced" AutomationProperties.Name="Advanced settings">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10">
                        <!-- Updates Group -->
                        <GroupBox Header="_Updates" Padding="10" Margin="0,0,0,15"
                                  AutomationProperties.Name="Update settings">
                            <StackPanel>
                                <CheckBox Content="Do_wnload and install updates automatically"
                                          IsChecked="{Binding AutoUpdate, Mode=TwoWay}"
                                          AutomationProperties.Name="Download and install updates automatically"/>
                                <TextBlock Text="When off, the Help menu still reports available updates but nothing is downloaded or installed. Takes effect the next time QuickMail starts. Applies to installed copies; the portable exe never updates itself."
                                           Foreground="{DynamicResource Theme.TextSecondary}" FontSize="{DynamicResource Theme.FontSizeSmall}" Margin="0,3,0,8" TextWrapping="Wrap"/>
                                <CheckBox Content="Show a _message after an update has been installed"
                                          IsChecked="{Binding ShowUpdateInstalledAlerts, Mode=TwoWay}"
                                          AutomationProperties.Name="Show a message after an update has been installed"/>
                                <TextBlock Text="Shows the QuickMail Update Installed dialog once, on the first start after an update."
                                           Foreground="{DynamicResource Theme.TextSecondary}" FontSize="{DynamicResource Theme.FontSizeSmall}" Margin="0,3,0,0" TextWrapping="Wrap"/>
                            </StackPanel>
                        </GroupBox>

                        <!-- QuickMail Logging Group -->
                        <GroupBox Header="QuickMail _Logging" Padding="10" Margin="0,0,0,15"
                                  AutomationProperties.Name="QuickMail logging settings">
                            <StackPanel>
                                <CheckBox Content="Enable _logging"
                                          IsChecked="{Binding EnableLogging, Mode=TwoWay}"
                                          AutomationProperties.Name="Enable logging"/>
                                <Button x:Name="DeleteLogButton"
                                        Content="_Delete QuickMail log"
                                        HorizontalAlignment="Left"
                                        Margin="0,10,0,0"
                                        Click="DeleteLogButton_Click"
                                        AutomationProperties.Name="Delete QuickMail log"/>
                            </StackPanel>
                        </GroupBox>

                        <!-- Log Format Group -->
                        <GroupBox Header="_Log Format" Padding="10" Margin="0,0,0,15"
                                  AutomationProperties.Name="Log format settings">
                            <StackPanel KeyboardNavigation.TabNavigation="Once"
                                        KeyboardNavigation.DirectionalNavigation="Cycle">
                                <RadioButton Content="_Action first (recommended)"
                                             IsChecked="{Binding IsLogFormatActionFirst, Mode=TwoWay}"
                                             GroupName="LogFormat"
                                             Checked="RadioButton_Checked"
                                             AutomationProperties.Name="Action first"
                                             AutomationProperties.HelpText="Message then timestamp — easier to scan since the log is already chronological."/>
                                <TextBlock Text="Example: Sync completed: 42 messages  [2024-11-15 14:23:45.123]"
                                           Foreground="{DynamicResource Theme.TextSecondary}" FontSize="{DynamicResource Theme.FontSizeSmall}" Margin="16,2,0,8"
                                           FontFamily="{DynamicResource Theme.FontFamilyMono}"/>
                                <RadioButton Content="_Time first"
                                             IsChecked="{Binding IsLogFormatTimeFirst, Mode=TwoWay}"
                                             GroupName="LogFormat"
                                             Checked="RadioButton_Checked"
                                             AutomationProperties.Name="Time first"
                                             AutomationProperties.HelpText="Timestamp then message — the original log format."/>
                                <TextBlock Text="Example: 2024-11-15 14:23:45.123  Sync completed: 42 messages"
                                           Foreground="{DynamicResource Theme.TextSecondary}" FontSize="{DynamicResource Theme.FontSizeSmall}" Margin="16,2,0,0"
                                           FontFamily="{DynamicResource Theme.FontFamilyMono}"/>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Keyboard Shortcuts Tab -->
            <TabItem Header="_Keyboard Shortcuts" AutomationProperties.Name="Keyboard shortcuts">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Helper text -->
                    <TextBlock Grid.Row="0"
                               Text="Arrow to a command, then press Enter to assign a shortcut or Delete to restore the default."
                               TextWrapping="Wrap"
                               Margin="0,0,0,10"
                               Foreground="{DynamicResource Theme.TextSecondary}"/>

                    <!-- Hotkeys ListView -->
                    <ListView Grid.Row="1"
                              x:Name="HotkeyListView"
                              ItemsSource="{Binding HotkeyRows}"
                              SelectedItem="{Binding SelectedHotkey, Mode=TwoWay}"
                              SelectionMode="Single"
                              BorderThickness="1"
                              Margin="0,0,0,6"
                              KeyDown="HotkeyListView_KeyDown"
                              SelectionChanged="HotkeyListView_SelectionChanged"
                              AutomationProperties.Name="Keyboard shortcuts list">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="AutomationProperties.Name" Value="{Binding AccessibleName}"/>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Category" DisplayMemberBinding="{Binding Category}" Width="90"/>
                                <GridViewColumn Header="Command" DisplayMemberBinding="{Binding Title}" Width="200"/>
                                <GridViewColumn Header="Shortcut" DisplayMemberBinding="{Binding ActiveGesture}" Width="130"/>
                            </GridView>
                        </ListView.View>
                    </ListView>

                    <!-- Action buttons — Grid.Row="2" so Tab flows: list → Set → Restore → Save/Cancel -->
                    <StackPanel Grid.Row="2"
                                Orientation="Horizontal">
                        <Button x:Name="SetShortcutButton"
                                Content="Set Shortcut…"
                                Click="SetShortcutButton_Click"
                                IsEnabled="False"
                                Padding="6,3"
                                Margin="0,0,8,0"
                                AutomationProperties.Name="Set shortcut for selected command"/>
                        <Button x:Name="RestoreDefaultButton"
                                Content="Restore Default"
                                Click="RestoreDefaultButton_Click"
                                IsEnabled="False"
                                Padding="6,3"
                                AutomationProperties.Name="Restore default shortcut for selected command"/>
                    </StackPanel>

                </Grid>
            </TabItem>

            <!-- Windowing Tab -->
            <TabItem Header="_Windowing" AutomationProperties.Name="Windowing settings">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10">
                        <!-- Message open mode -->
                        <GroupBox Header="_Message Open Mode" Padding="10" Margin="0,0,0,15"
                                  AutomationProperties.Name="Message open mode settings">
                            <StackPanel>
                                <TextBlock TextWrapping="Wrap" Margin="0,0,0,8" Foreground="{DynamicResource Theme.TextSecondary}" FontSize="{DynamicResource Theme.FontSizeSmall}"
                                           Text="Choose where messages open when you press Enter or select one."/>
                                <StackPanel KeyboardNavigation.TabNavigation="Once"
                                            KeyboardNavigation.DirectionalNavigation="Cycle">
                                    <RadioButton Content="Reading _pane (default)"
                                                 IsChecked="{Binding IsReadingPaneMode, Mode=TwoWay}"
                                                 GroupName="MessageOpenMode"
                                                 Checked="RadioButton_Checked"
                                                 AutomationProperties.Name="Reading pane"
                                                 AutomationProperties.HelpText="Opens messages in the reading pane below the message list. This is the default behaviour."/>
                                    <TextBlock Text="Enter opens a message in the reading pane below the list."
                                               Foreground="{DynamicResource Theme.TextSecondary}" FontSize="{DynamicResource Theme.FontSizeSmall}" Margin="16,2,0,8"/>
                                    <RadioButton Content="_Tab"
                                                 IsChecked="{Binding IsTabMode, Mode=TwoWay}"
                                                 GroupName="MessageOpenMode"
                                                 Checked="RadioButton_Checked"
                                                 AutomationProperties.Name="Tab"
                                                 AutomationProperties.HelpText="Opens messages in a tab strip above the message list."/>
                                    <TextBlock Text="Enter opens a message as a new tab. Ctrl+Tab cycles tabs. Ctrl+W closes."
                                               TextWrapping="Wrap"
                                               Foreground="{DynamicResource Theme.TextSecondary}" FontSize="{DynamicResource Theme.FontSizeSmall}" Margin="16,2,0,8"/>
                                    <RadioButton Content="_Window"
                                                 IsChecked="{Binding IsWindowMode, Mode=TwoWay}"
                                                 GroupName="MessageOpenMode"
                                                 Checked="RadioButton_Checked"
                                                 AutomationProperties.Name="Window"
                                                 AutomationProperties.HelpText="Opens each message in its own window."/>
                                    <TextBlock Text="Enter opens a message in a new window. Use Ctrl+Enter in any mode to always open in a new window."
                                               TextWrapping="Wrap"
                                               Foreground="{DynamicResource Theme.TextSecondary}" FontSize="{DynamicResource Theme.FontSizeSmall}" Margin="16,2,0,0"/>
                                </StackPanel>
                            </StackPanel>
                        </GroupBox>

                        <!-- Tab behaviour -->
                        <GroupBox Header="_Tab Behaviour" Padding="10" Margin="0,0,0,0"
                                  AutomationProperties.Name="Tab behaviour settings">
                            <StackPanel>
                                <CheckBox Content="_Confirm before closing a tab with unsaved changes"
                                          IsChecked="{Binding ConfirmCloseTabWithUnsaved, Mode=TwoWay}"
                                          AutomationProperties.Name="Show a confirmation dialog before closing a tab that has unsaved draft changes"/>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            <!-- Appearance Tab -->
            <TabItem Header="A_ppearance" AutomationProperties.Name="Appearance">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10">
                        <!-- High Contrast notice — visible only while Windows HC is active. -->
                        <TextBlock Text="Windows High Contrast is active; theme colors are supplied by Windows. Your theme choice below is saved and returns when High Contrast is turned off."
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,10"
                                   Visibility="{Binding IsHighContrastActive, Converter={StaticResource BoolToVisibilityConverter}}"/>

                        <!-- Theme Group -->
                        <GroupBox Header="_Theme" Padding="10" Margin="0,0,0,15"
                                  AutomationProperties.Name="Theme settings">
                            <StackPanel>
                                <Label x:Name="ThemeLabel"
                                       Content="The_me:"
                                       Target="{Binding ElementName=ThemeCombo}"
                                       FontWeight="SemiBold"/>
                                <ComboBox x:Name="ThemeCombo"
                                          ItemsSource="{Binding ThemeOptions}"
                                          DisplayMemberPath="Name"
                                          SelectedValuePath="Id"
                                          SelectedValue="{Binding AppearanceThemeId, Mode=TwoWay}"
                                          AutomationProperties.LabeledBy="{Binding ElementName=ThemeLabel}"/>
                                <TextBlock Text="System follows the Windows light or dark setting. Themes can be managed, shared, and imported from the Theme Manager (Command Palette: Manage Themes)."
                                           TextWrapping="Wrap"
                                           Foreground="{DynamicResource Theme.TextSecondary}"
                                           FontSize="{DynamicResource Theme.FontSizeSmall}"
                                           Margin="0,4,0,0"/>
                            </StackPanel>
                        </GroupBox>

                        <!-- Text Group -->
                        <GroupBox Header="Te_xt" Padding="10" Margin="0,0,0,15"
                                  AutomationProperties.Name="Text settings">
                            <StackPanel>
                                <Label x:Name="FontLabel"
                                       Content="_Font:"
                                       Target="{Binding ElementName=FontCombo}"
                                       FontWeight="SemiBold"/>
                                <ComboBox x:Name="FontCombo"
                                          ItemsSource="{Binding FontOptions}"
                                          SelectedItem="{Binding AppearanceFontOption, Mode=TwoWay}"
                                          AutomationProperties.LabeledBy="{Binding ElementName=FontLabel}"/>

                                <StackPanel Margin="0,8,0,0">
                                    <Label x:Name="TextSizeLabel"
                                           Content="Text si_ze:"
                                           Target="{Binding ElementName=TextSizeCombo}"
                                           FontWeight="SemiBold"/>
                                    <ComboBox x:Name="TextSizeCombo"
                                              SelectedValue="{Binding AppearanceTextScalePercent, Mode=TwoWay}"
                                              SelectedValuePath="Tag"
                                              AutomationProperties.LabeledBy="{Binding ElementName=TextSizeLabel}">
                                        <ComboBoxItem Tag="100" Content="100%"/>
                                        <ComboBoxItem Tag="110" Content="110%"/>
                                        <ComboBoxItem Tag="125" Content="125%"/>
                                        <ComboBoxItem Tag="150" Content="150%"/>
                                        <ComboBoxItem Tag="175" Content="175%"/>
                                        <ComboBoxItem Tag="200" Content="200%"/>
                                    </ComboBox>
                                </StackPanel>
                            </StackPanel>
                        </GroupBox>

                        <!-- Vision Group -->
                        <GroupBox Header="_Vision" Padding="10"
                                  AutomationProperties.Name="Vision settings">
                            <StackPanel>
                                <CheckBox Content="Always _underline links"
                                          IsChecked="{Binding AppearanceUnderlineLinks, Mode=TwoWay}"
                                          AutomationProperties.Name="Always underline links"/>
                                <CheckBox Content="Thicker keyboard focus in_dicators"
                                          IsChecked="{Binding AppearanceThickFocus, Mode=TwoWay}"
                                          Margin="0,6,0,0"
                                          AutomationProperties.Name="Thicker keyboard focus indicators"/>
                                <CheckBox Content="Apply theme colors to message c_ontent"
                                          IsChecked="{Binding AppearanceForceMessageTheme, Mode=TwoWay}"
                                          Margin="0,6,0,0"
                                          AutomationProperties.Name="Apply theme colors to message content"/>
                                <TextBlock Text="Overrides the colors and fonts chosen by a message's sender. Turn on when messages arrive with hard-to-read colors."
                                           TextWrapping="Wrap"
                                           Foreground="{DynamicResource Theme.TextSecondary}"
                                           FontSize="{DynamicResource Theme.FontSizeSmall}"
                                           Margin="0,3,0,0"/>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>

        <!-- Buttons -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    >
            <Button x:Name="SaveButton"
                    Content="_Save"
                    IsDefault="True"
                    Click="SaveButton_Click"
                    MinWidth="75"
                    AutomationProperties.Name="Save settings"/>
            <Button Content="_Cancel"
                    IsCancel="True"
                    MinWidth="75"
                    AutomationProperties.Name="Cancel and close without saving"/>
        </StackPanel>

    </Grid>
</Window>
