<Window x:Class="QuickMail.Views.TabListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Open Tabs"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        Width="500"
        SizeToContent="Height"
        MaxHeight="400"
        WindowStartupLocation="Manual"
        ShowInTaskbar="False"
        PreviewKeyDown="OnPreviewKeyDown"
        Loaded="OnLoaded">

    <Border Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"
            BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"
            BorderThickness="1"
            CornerRadius="4">
        <Border.Effect>
            <DropShadowEffect BlurRadius="12" ShadowDepth="3" Opacity="0.35" Direction="270"/>
        </Border.Effect>
        <DockPanel>
            <TextBlock DockPanel.Dock="Top"
                       x:Name="HeaderText"
                       Text="Open tabs"
                       FontWeight="SemiBold"
                       Padding="10,8,10,4"
                       Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>

            <ListBox x:Name="TabList"
                     Margin="4,0,4,4"
                     MaxHeight="360"
                     BorderThickness="0"
                     Background="Transparent"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     AutomationProperties.Name="Open tabs. Arrow keys to navigate, Enter to activate, Delete to close, Escape to cancel."
                     MouseDoubleClick="TabList_MouseDoubleClick">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="8,5"/>
                        <Setter Property="IsTabStop" Value="False"/>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       Text="{Binding Title}"
                                       TextTrimming="CharacterEllipsis"
                                       VerticalAlignment="Center"/>
                            <Button Grid.Column="1"
                                    Content="✕"
                                    Width="20" Height="20"
                                    Padding="0"
                                    Margin="8,0,0,0"
                                    FontSize="10"
                                    Tag="{Binding}"
                                    Click="CloseTabButton_Click"
                                    AutomationProperties.Name="Close this tab"
                                    ToolTip="Close tab"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </DockPanel>
    </Border>
</Window>
